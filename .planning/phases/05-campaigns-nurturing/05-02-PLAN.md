---
phase: 05-campaigns-nurturing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - types/campaign.ts
  - lib/stores/sequences.ts
  - components/campaigns/SequenceStepForm.tsx
  - components/campaigns/SequenceList.tsx
  - components/campaigns/SequenceFlowChart.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can add email step with template selection"
    - "Admin can add WhatsApp step with template selection"
    - "Admin can add delay step with relative (minutes after previous) or absolute (specific time) scheduling"
    - "Admin can edit existing sequence step details via modal"
    - "Admin can delete sequence steps from sequence"
    - "Admin can reorder sequence steps (up/down buttons)"
    - "Admin can toggle between flow chart view and table view of sequence"
  artifacts:
    - path: "types/campaign.ts"
      provides: "Sequence builder TypeScript interfaces"
      exports: ["SequenceStep", "DelayType", "StepType", "BuilderViewMode", "SequenceBuilderState", "StepFormData"]
    - path: "lib/stores/sequences.ts"
      provides: "Zustand store for sequence state management"
      exports: ["useSequenceStore"]
    - path: "components/campaigns/SequenceStepForm.tsx"
      provides: "Modal form for editing sequence steps"
      contains: "step type, template, delay settings"
    - path: "components/campaigns/SequenceList.tsx"
      provides: "Table view of sequence steps"
      contains: "step rows, actions, reorder controls"
    - path: "components/campaigns/SequenceFlowChart.tsx"
      provides: "Flow chart visualization component"
      contains: "visual step cards, connecting arrows"
  key_links:
    - from: "components/campaigns/SequenceStepForm.tsx"
      to: "lib/stores/sequences.ts"
      via: "Store for step form state"
      pattern: "useSequenceStore|openStepForm|updateStep"
    - from: "components/campaigns/SequenceList.tsx"
      to: "lib/stores/sequences.ts"
      via: "Store for list actions"
      pattern: "deleteStep|reorderStep|onEditStep"
    - from: "components/campaigns/SequenceFlowChart.tsx"
      to: "types/campaign.ts"
      via: "Step type definitions"
      pattern: "SequenceStep|StepType"
---

<objective>
Create sequence builder foundation components: step form, list view, flow chart visualization, and state management store.

**Purpose:** Build core UI components for sequence step management with modal editing, table/flow chart views, and reordering capabilities.

**Output:**
- Sequence builder TypeScript types and interfaces
- Zustand store for sequence state management
- Modal form for adding/editing steps with type selection and delay configuration
- Table view component for step list with actions
- Flow chart visualization component for visual sequence representation
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-campaigns-nurturing/05-CONTEXT.md
@.planning/phases/05-campaigns-nurturing/05-01-SUMMARY.md
@types/campaign.ts (Campaign and Sequence types from Plan 01)
@lib/api/campaigns.ts (Campaign API)
@lib/api/email-templates.ts (Email templates for selection)
@components/admin/emails/EmailForm.tsx (Form patterns)
@components/admin/qa/QaPage.tsx (List and action patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Sequence types and add builder-specific interfaces</name>
  <files>types/campaign.ts</files>
  <action>
    1. Update types/campaign.ts with sequence builder types:

       // Builder state
       enum BuilderViewMode { FlowChart = 'flowchart', Table = 'table' }

       interface SequenceBuilderState {
         campaign_id?: string;
         sequence_id?: string;
         name: string;
         steps: SequenceStep[];
         viewMode: BuilderViewMode;
         isDirty: boolean;
       }

       interface StepValidationError {
         step_id: string;
         field: string;
         message: string;
       }

       // Step edit form state
       interface StepFormData {
         type: StepType;
         template_id?: string;
         delay_type?: DelayType;
         delay_minutes?: number;
         scheduled_time?: string;
       }

       // Reorder action
       interface ReorderAction {
         from_index: number;
         to_index: number;
       }

    2. Export all new types and interfaces
    3. Ensure CampaignStep from Plan 01 has all required fields
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
  </verify>
  <done>
    Sequence builder types added with view mode and form state interfaces.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sequence Zustand store</name>
  <files>lib/stores/sequences.ts</files>
  <action>
    1. Create lib/stores/sequences.ts Zustand store:

       State:
       - sequences: Sequence[]
       - activeSequence: Sequence | null
       - builderState: SequenceBuilderState | null
       - isStepFormOpen: boolean
       - editingStepIndex: number | null
       - validationErrors: StepValidationError[]

       Actions:
       - fetchSequences(campaign_id): Load all sequences for campaign
       - setActiveSequence(sequence): Set current sequence
       - initBuilder(campaign_id?, sequence?): Initialize builder state
       - addStep(step): Add step to builder
       - updateStep(index, step): Update existing step
       - deleteStep(index): Remove step from sequence
       - reorderStep(from_index, to_index): Move step to new position
       - setViewMode(mode): Toggle between flow chart and table
       - openStepForm(index): Open edit modal for step
       - closeStepForm(): Close edit modal
       - saveSequence(): Persist sequence to API
       - discardChanges(): Reset builder to saved state
       - validateSequence(): Check for errors (circular deps, missing templates)
       - resetBuilder(): Clear builder state

       Use persist middleware for builder state (prevent data loss on refresh)
    2. Connect to campaign API from lib/api/campaigns.ts
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
    Store state and actions properly typed
  </verify>
  <done>
    Sequence store created with builder state management and step operations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SequenceStepForm modal component</name>
  <files>components/campaigns/SequenceStepForm.tsx</files>
  <action>
    1. Create components/campaigns/SequenceStepForm.tsx:

    Props:
    - isOpen: boolean
    - onClose: () => void
    - onSave: (step: SequenceStep) => void
    - step?: SequenceStep (for editing)
    - stepType?: 'email' | 'whatsapp' | 'delay' (for new steps)

    UI - Step type section (if creating new step):
    - Radio group or card selection for step type
    - Email icon + "Email Adımı"
    - WhatsApp icon + "WhatsApp Adımı"
    - Clock icon + "Gecikme Adımı"

    UI - Email/WhatsApp step content:
    - Template select dropdown (fetch from email-templates API)
    - Template preview (show subject/body summary)
    - Required field validation

    UI - Delay step content:
    - Delay type toggle: "Adım Sonrası" vs "Mutlak Zaman"
    - If "Adım Sonrası" (relative):
      - Number input: minutes (default 60)
      - Quick select buttons: 15 dk, 1 saat, 1 gün, 1 hafta
    - If "Mutlak Zaman" (absolute):
      - Date/time picker input
      - Timezone selector (default: Europe/Istanbul)
      - Warning: "Mutlak zamanlama, lead'in kayıt zamanına değil belirtilen zamana göre çalışır"

    Styling:
    - Use Dialog from @radix-ui/react-dialog
    - Form layout with clear sections
    - Turkish labels
    - Required field indicators

    Reference components/admin/emails/EmailForm.tsx for form patterns

    Edit workflow (for existing steps):
    - Click existing step → open modal with step data pre-populated
    - Save changes → update step in sequence via store.updateStep()
    - Modal closes and step reflects changes
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
    Form renders and validation works
    Edit workflow: click step → modal opens with data → save → updates sequence
  </verify>
  <done>
    Sequence step form modal created with type selection, delay configuration, and edit workflow.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create SequenceList table view component</name>
  <files>components/campaigns/SequenceList.tsx</files>
  <action>
    1. Create components/campaigns/SequenceList.tsx:

    Props:
    - steps: SequenceStep[]
    - onEditStep: (index: number) => void
    - onDeleteStep: (index: number) => void
    - onReorderStep: (from: number, to: number) => void

    UI - Table view:
    - Columns: Sıra, Tür, İçerik, Gecikme, İşlemler
    - Sıra column: Step order number (1, 2, 3...)
    - Tür column: Icon + label (Email/WhatsApp/Gecikme)
    - İçerik column: Template name for email/WhatsApp, "-" for delay
    - Gecikme column: Delay value (e.g., "1 saat", "2026-03-10 09:00")
    - İşlemler column: Edit button, Delete button, Up arrow, Down arrow

    Row actions:
    - Edit: Open SequenceStepForm modal with step data
    - Delete: Show confirmation dialog, then remove
    - Up/Down arrows: Move step up or down in sequence (disabled for first/last)

    Empty state:
    - "Henüz adım eklenmedi"
    - "Adım Ekle" button

    Styling:
    - Use Table from shadcn/ui
    - Hover effects on rows
    - Action buttons with tooltips
    - Turkish language

    Reference components/leads/LeadList.tsx for table patterns
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
    Component renders and actions work
  </verify>
  <done>
    Sequence list component created with table view and row actions.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create flow chart visualization component</name>
  <files>components/campaigns/SequenceFlowChart.tsx</files>
  <action>
    1. Create components/campaigns/SequenceFlowChart.tsx:

    Props:
    - steps: SequenceStep[]
    - onEditStep: (index: number) => void
    - onDeleteStep: (index: number) => void

    UI - Flow chart view:
    - Vertical layout with connecting lines
    - Each step as a card:
      - Header: Step type icon + label
      - Body: Template name or delay info
      - Footer: Edit button (pencil icon)
    - Arrow connecting each step to next
    - Last step shows "Bitiş" indicator

    Interactions:
    - Click step card to edit (opens SequenceStepForm with step data)
    - Hover effects show edit/delete buttons
    - Drag handle for reordering (optional, if implementing drag-drop)

    Styling:
    - Use lucide-react icons (Mail, MessageSquare, Clock, ArrowDown, Edit, Trash2)
    - Card components from shadcn/ui
    - Vertical line using CSS border-left
    - Color coding: blue (email), green (WhatsApp), gray (delay)

    Responsive:
    - Stack horizontally on mobile
    - Vertical on desktop

    Reference components/leads/WhatsAppTab.tsx for timeline patterns
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
    Flow chart renders and interactions work
  </verify>
  <done>
    Flow chart component created with visual step representation.
  </done>
</task>

</tasks>

<verification>
1. Builder types defined with all required interfaces
2. Store created with step management actions
3. Step form modal opens for new steps
4. Step form modal opens for editing existing steps with data pre-populated
5. Saving edited step updates sequence in store
6. Table view shows all steps with correct actions
7. Flow chart shows visual step cards
8. Reorder buttons move steps correctly
9. Delete removes step after confirmation
</verification>

<success_criteria>
- Sequence builder types and store created
- Step form modal supports add and edit workflows
- Table view displays steps with actions
- Flow chart visualizes sequence steps
- Step editing via modal works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-campaigns-nurturing/05-02-SUMMARY.md`
</output>
