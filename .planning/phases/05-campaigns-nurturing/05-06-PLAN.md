---
phase: 05-campaigns-nurturing
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - components/campaigns/SequenceBuilder.tsx
  - app/(dashboard)/campaigns/[id]/sequences/page.tsx
  - components/campaigns/CampaignForm.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can create sequence with multiple steps for a campaign"
    - "Admin can toggle between flow chart view and table view of sequence"
    - "Admin can add, edit, delete, and reorder sequence steps"
    - "Sequence builder accessible from dedicated sequences page"
    - "Sequence builder integrated into campaign form for inline creation"
    - "Sequence saves with campaign when created inline"
  artifacts:
    - path: "components/campaigns/SequenceBuilder.tsx"
      provides: "Main sequence builder component with view toggle"
      contains: "flow chart, table view, step management, save/cancel"
    - path: "app/(dashboard)/campaigns/[id]/sequences/page.tsx"
      provides: "Sequence management page"
      contains: "sequence list, builder, step CRUD, view toggle"
    - path: "components/campaigns/CampaignForm.tsx"
      provides: "Updated campaign form with sequence integration"
      contains: "inline sequence builder, sequence summary"
  key_links:
    - from: "components/campaigns/SequenceBuilder.tsx"
      to: "components/campaigns/SequenceList.tsx"
      via: "Table view for step management"
      pattern: "SequenceList|viewMode"
    - from: "components/campaigns/SequenceBuilder.tsx"
      to: "components/campaigns/SequenceFlowChart.tsx"
      via: "Flow chart visualization"
      pattern: "SequenceFlowChart|viewMode"
    - from: "components/campaigns/SequenceBuilder.tsx"
      to: "lib/stores/sequences.ts"
      via: "Zustand store for sequence state"
      pattern: "useSequenceStore|saveSequence|discardChanges"
    - from: "app/(dashboard)/campaigns/[id]/sequences/page.tsx"
      to: "lib/api/campaigns.ts"
      via: "Sequence CRUD operations"
      pattern: "createSequence|updateSequence|deleteSequence"
    - from: "components/campaigns/CampaignForm.tsx"
      to: "components/campaigns/SequenceBuilder.tsx"
      via: "Integration for inline sequence creation"
      pattern: "SequenceBuilder|inline"
---

<objective>
Create main sequence builder component with view toggle, sequence management page, and integrate builder into campaign form for inline sequence creation.

**Purpose:** Provide complete sequence building experience through dedicated page and inline integration, enabling admins to create and manage multi-step sequences within campaigns.

**Output:**
- Main SequenceBuilder component orchestrating all sub-components
- View toggle between flow chart and table modes
- Sequence management page with list and builder
- Campaign form integration with inline sequence creation
- Save/cancel functionality with validation
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-campaigns-nurturing/05-CONTEXT.md
@.planning/phases/05-campaigns-nurturing/05-01-SUMMARY.md
@.planning/phases/05-campaigns-nurturing/05-02-SUMMARY.md
@types/campaign.ts (Campaign and Sequence types)
@lib/api/campaigns.ts (Campaign API)
@lib/stores/sequences.ts (Sequence store from Plan 02)
@components/campaigns/SequenceList.tsx (Table view from Plan 02)
@components/campaigns/SequenceFlowChart.tsx (Flow chart from Plan 02)
@components/campaigns/SequenceStepForm.tsx (Step form from Plan 02)
@components/admin/qa/QaPage.tsx (List and action patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main SequenceBuilder component</name>
  <files>components/campaigns/SequenceBuilder.tsx</files>
  <action>
    1. Create components/campaigns/SequenceBuilder.tsx:

    Props:
    - campaignId?: string (for new sequences)
    - sequenceId?: string (for editing existing)
    - inline?: boolean (for campaign form integration, default: false)

    State:
    - ViewMode: 'flowchart' | 'table' (default: table)
    - Steps: SequenceStep[] (from store)

    UI - Header:
    - Sequence name input (editable)
    - View toggle: Switch between flow chart and table icons
    - "Adım Ekle" dropdown menu: Email, WhatsApp, Delay options

    UI - Main content (conditional):
    - If table mode: Show SequenceList component
    - If flow chart mode: Show SequenceFlowChart component

    UI - Footer:
    - Step count indicator
    - "Kaydet" button (primary)
    - "İptal" button (discard changes)
    - Validation warnings (if any)

    Actions:
    - Add step: Open SequenceStepForm with new step mode
    - Edit step: Open SequenceStepForm with existing step data
    - Delete step: Show confirmation, remove from array
    - Reorder: Call store's reorderStep action
    - Save: Call store's saveSequence, show success toast
    - Cancel: Call store's discardChanges, show warning toast

    Styling:
    - Use shadcn/ui components (Button, Switch, Input)
    - Tailwind for layout
    - Responsive design

    Integration for inline mode:
    - If inline=true: Hide save/cancel buttons (handled by parent form)
    - If inline=true: Simplified header (no name input if campaign handles it)
    - If inline=false: Full-featured builder for dedicated page

    Use useSequenceStore for state management
    Use sonner for toast notifications
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
    Builder renders and all actions work
    View toggle switches between table and flow chart
    Inline mode adjusts UI correctly
  </verify>
  <done>
    Sequence builder component created with view toggle, step management, and inline support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sequence management page</name>
  <files>app/(dashboard)/campaigns/[id]/sequences/page.tsx</files>
  <action>
    1. Create app/(dashboard)/campaigns/[id]/sequences/page.tsx:

    Server component:
    - Fetch campaign by ID
    - Fetch all sequences for campaign
    - Check permissions (CAN_MANAGE_CAMPAIGNS)

    UI - Page layout:
    - Breadcrumb: Campaigns > Campaign Name > Sequences
    - Page header: Campaign name, "Yeni Sequence" button
    - Sequence list (if multiple):
      - Cards showing sequence name, step count, status
      - "Düzenle" button opens SequenceBuilder
      - "Sil" button (with confirmation)
    - Active sequence editor:
      - SequenceBuilder component (inline=false)
      - Full width, centered

    Client component wrapper for interactivity

    Routing:
    - /campaigns/[id]/sequences: List all sequences
    - /campaigns/[id]/sequences?edit=[seq-id]: Edit specific sequence
    - /campaigns/[id]/sequences?new: Create new sequence

    Reference app/(dashboard)/campaigns/page.tsx for layout patterns
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
    Page loads and sequences display correctly
    Builder opens for new/edit sequences
  </verify>
  <done>
    Sequence management page created with list and builder.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate SequenceBuilder into CampaignForm</name>
  <files>components/campaigns/CampaignForm.tsx</files>
  <action>
    1. Update components/campaigns/CampaignForm.tsx:

    Add section after campaign basic fields:
    - "Sequence Adımları" section header
    - Inline SequenceBuilder component (collapsed by default)
    - Toggle: "Şimdi Sequence Oluştur" checkbox
      - If checked: Show SequenceBuilder (inline=true)
      - If unchecked: Hide, sequence can be created later

    Integration points:
    - When campaign saves, also save sequence if builder has steps
    - Pass campaign_id to SequenceBuilder after campaign creation
    - Show sequence summary on campaign list view

    Form flow:
    1. Admin fills campaign details (name, type, segment)
    2. Optional: Check "Şimdi Sequence Oluştur"
    3. SequenceBuilder expands below
    4. Admin adds steps (opens modals, reorders, deletes)
    5. "Kaydet" saves both campaign and sequence

    Validation:
    - If "Şimdi Sequence Oluştur" checked: require at least 1 step
    - Show validation error if no steps added
    - Validate all steps have required fields (template_id for email/WhatsApp, delay for delay steps)

    Update form submission to handle sequence creation:
    - After campaign created: get campaign_id
    - If builder has steps: call createSequence with campaign_id
    - Show success message with sequence info
    - If sequence creation fails: still save campaign, show warning

    Update campaign list view:
    - Show sequence count in campaign list
    - Show first sequence name if exists
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
    Integration works, sequence saves with campaign
    Validation requires at least 1 step when toggle checked
  </verify>
  <done>
    SequenceBuilder integrated into campaign creation flow with validation and save handling.
  </done>
</task>

</tasks>

<verification>
1. Sequence builder opens with empty state
2. Add Email step works, template select shows templates
3. Add WhatsApp step works, template select shows templates
4. Add Delay step works, both delay types selectable
5. Relative delay: minutes input works
6. Absolute delay: date/time picker works
7. Edit step opens modal with existing data
8. Delete step removes with confirmation
9. Reorder steps: Up/down buttons move steps correctly
10. View toggle switches between table and flow chart
11. Table view shows all step details in columns
12. Flow chart shows visual step cards with arrows
13. Sequence saves and appears in list
14. Integration: Campaign form includes sequence builder
15. Sequence saves together with campaign
16. Inline mode shows simplified UI
17. Dedicated page shows full-featured builder
</verification>

<success_criteria>
- Admin can create multi-step sequences
- All step types (email, WhatsApp, delay) supported
- Step editing via modal works
- Step deletion with confirmation works
- Step reordering works
- View toggle (flow chart/table) works
- Delay type selection (relative/absolute) works
- Sequence builder integrated into campaign form
- Sequence persists correctly with campaign
- Dedicated sequences page functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-campaigns-nurturing/05-06-SUMMARY.md`
</output>
