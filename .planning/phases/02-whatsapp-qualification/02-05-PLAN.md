---
phase: 02-whatsapp-qualification
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pb_migrations/*_updated_leads.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "leads collection has qa_sent boolean field"
    - "leads collection has qa_sent_at date field"
    - "leads collection has qa_completed boolean field"
    - "leads collection has qa_completed_at date field"
    - "leads collection has total_score number field"
  artifacts:
    - path: "pb_migrations/*_updated_leads.js"
      provides: "Migration adding 5 Phase 2 fields to leads collection"
      contains: "collection.fields.addAt"
  key_links:
    - from: "types/lead.ts"
      to: "pb_migrations/*_updated_leads.js"
      via: "TypeScript interface definition matches database schema"
      pattern: "qa_sent|qa_sent_at|qa_completed|qa_completed_at|total_score"
---

<objective>
Add missing Phase 2 fields to the leads collection to align PocketBase schema with TypeScript types.

Purpose: Close critical data model gap where Phase 2 code references fields that don't exist in the database, causing runtime errors when trying to update lead QA status and scores.

Output: New PocketBase migration adding 5 fields to leads collection (qa_sent, qa_sent_at, qa_completed, qa_completed_at, total_score)
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-whatsapp-qualification/02-VERIFICATION.md
@types/lead.ts
@pb_migrations/1772478433_created_leads.js

# Gap Summary
The TypeScript interface Lead (types/lead.ts) defines 5 Phase 2 fields that do not exist in the PocketBase leads collection:
- qa_sent (boolean) - tracks if QA poll was sent
- qa_sent_at (date) - timestamp when poll was sent
- qa_completed (boolean) - tracks if QA flow is complete
- qa_completed_at (date) - timestamp when QA completed
- total_score (number) - accumulated score from QA answers

All Phase 2 code references these fields, causing failures at runtime.
</context>

<tasks>

<task type="auto">
  <name>Create migration to add Phase 2 fields to leads collection</name>
  <files>pb_migrations/*_updated_leads.js</files>
  <action>
    Create a new PocketBase migration file (use current timestamp as filename prefix) that adds 5 fields to the leads collection:

    1. Find the leads collection by ID: app.findCollectionByNameOrId("pbc_3705076665")

    2. Add fields in order using collection.fields.addAt(position, new Field({...})):
       - Position 11 (after "quality" field): qa_sent (bool, default: false)
       - Position 12: qa_sent_at (date, optional)
       - Position 13: qa_completed (bool, default: false)
       - Position 14: qa_completed_at (date, optional)
       - Position 15: total_score (number, optional, no min/max)

    3. Field configurations:
       - qa_sent: type="bool", hidden=false, required=false, system=false
       - qa_sent_at: type="date", hidden=false, required=false, system=false
       - qa_completed: type="bool", hidden=false, required=false, system=false
       - qa_completed_at: type="date", hidden=false, required=false, system=false
       - total_score: type="number", hidden=false, required=false, system=false, onlyInt=false, no min/max constraints

    4. Write rollback in down() function that removes all 5 fields

    5. IMPORTANT: Use unique field IDs - generate random 10-digit strings for each field (e.g., "bool1234567890", "date9876543210")

    Reference existing migrations for pattern:
    - pb_migrations/1772463186_updated_users.js shows addAt pattern
    - types/lead.ts lines 36-39 show expected field names and types
  </action>
  <verify>
    After creating migration:
    1. Check migration file exists: ls pb_migrations/*_updated_leads.js
    2. Verify migration syntax: File contains migrate() with up() and down() functions
    3. Run migration: pocketbase migrate (if available) or check PocketBase admin UI that fields were added
  </verify>
  <done>
    Migration file created with all 5 fields added in up() and removed in down(). PocketBase schema matches TypeScript interface (types/lead.ts lines 36-39).
  </done>
</task>

</tasks>

<verification>
Run migration and verify fields exist in PocketBase:
1. Check migration runs without errors
2. Verify in PocketBase Admin UI: Settings > Database > leads collection shows new fields
3. Confirm TypeScript types align with schema (qa_sent: bool, qa_sent_at: date, etc.)
</verification>

<success_criteria>
1. Migration file created with valid PocketBase migration syntax
2. All 5 fields added to leads collection schema
3. Rollback function removes all 5 fields
4. Migration can be applied via pocketbase migrate command
5. No TypeScript errors related to lead field types
</success_criteria>

<output>
After completion, create `.planning/phases/02-whatsapp-qualification/02-05-SUMMARY.md`
</output>
