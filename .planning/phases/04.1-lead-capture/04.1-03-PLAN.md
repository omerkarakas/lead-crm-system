---
phase: 04.1-lead-capture
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - app/lead-form/page.tsx
  - app/api/leads/route.ts
  - types/lead.ts
  - pb_migrations/XXXX_add_utm_fields_to_leads.js
autonomous: true

must_haves:
  truths:
    - "Public can access /lead-form page without authentication"
    - "Form accepts name, phone, email, company, website, message with same validation as admin form"
    - "Form submission creates lead with source='web_form'"
    - "UTM parameters (utm_source, utm_medium, utm_campaign) are captured and stored"
    - "Duplicate leads (matching phone or email) update existing record, set status to 're-apply'"
  artifacts:
    - path: "app/lead-form/page.tsx"
      provides: "Public lead form page component"
      contains: "name, phone, email, company, website, message fields"
    - path: "app/api/leads/route.ts"
      provides: "Enhanced lead creation with UTM and duplicate handling"
      exports: ["POST"]
    - path: "types/lead.ts"
      provides: "Type definitions with UTM fields"
      contains: "utm_source, utm_medium, utm_campaign, utm_content, utm_term"
  key_links:
    - from: "app/lead-form/page.tsx"
      to: "/api/leads"
      via: "POST request with form data"
      pattern: "fetch.*api/leads.*POST"
    - from: "app/api/leads/route.ts"
      to: "leads collection"
      via: "PocketBase create or update"
      pattern: "pb\.collection\('leads'\)\.(create|update)"
---

<objective>
Create public lead capture form with UTM tracking and duplicate handling.

Purpose: Allow leads to submit their information via public web form. The form captures standard lead fields plus marketing attribution (UTM parameters) for campaign tracking. Duplicate submissions update existing leads and mark them as 're-apply' to preserve engagement history.
Output: Public /lead-form page with enhanced lead creation API
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@components/leads/LeadForm.tsx
@app/api/leads/route.ts
@types/lead.ts
</context>

<tasks>

<task type="auto">
  <name>Add UTM fields to leads collection and types</name>
  <files>
    pb_migrations/XXXX_add_utm_fields_to_leads.js
    types/lead.ts
  </files>
  <action>
    1. Create PocketBase migration to add UTM fields to leads collection:
       - utm_source (text, optional) - Campaign source (facebook, google, etc.)
       - utm_medium (text, optional) - Marketing medium (cpc, banner, etc.)
       - utm_campaign (text, optional) - Campaign name/identifier
       - utm_content (text, optional) - Content identifier for A/B testing
       - utm_term (text, optional) - Search terms for paid search
       - utm_timestamp (date, optional) - When UTM params were captured

    2. Update Lead and CreateLeadDto interfaces in types/lead.ts to include these fields

    3. Run migration: `npx pocketbase migrate`

    Field specifications:
    - All fields are optional (not all traffic will have UTM params)
    - Text fields with max length 200 for campaign identifiers
    - utm_timestamp defaults to created date if not provided

    Reference existing leads collection schema in pb_schema.json for migration structure.
  </action>
  <verify>grep "utm_source" pb_data/pb_schema.json | grep leads returns the field definition</verify>
  <done>Leads collection has UTM tracking fields, TypeScript types updated</done>
</task>

<task type="auto">
  <name>Enhance POST /api/leads with duplicate handling</name>
  <files>app/api/leads/route.ts</files>
  <action>
    Enhance the existing POST /api/leads endpoint to handle duplicates and UTM parameters:

    1. Add UTM parameter extraction from request body:
       - Check for utm_source, utm_medium, utm_campaign, utm_content, utm_term
       - Add utm_timestamp if any UTM params present

    2. Add duplicate detection before creating new lead:
       - Query leads collection for matching phone OR email
       - Use filter: `phone = "${phone}" || email = "${email}"`
       - If match found: UPDATE existing lead instead of creating new

    3. For duplicate leads, implement update logic:
       - Store old values in notes field with timestamp: "TEKRAR BASVURU [DD.MM.YYYY]: Eski değerler - name: X, phone: Y..."
       - Update with new form data (overwrite fields)
       - Set status to 're-apply'
       - Set source to 'web_form' (new submission source)
       - Append UTM params to existing UTM fields or create new entry
       - Reset qa_completed flag to false (requires re-qualification)

    4. For new leads (no duplicate):
       - Create as normal with existing logic
       - Set source to 'web_form'
       - Include UTM parameters if provided

    5. Response should indicate whether lead was created or updated:
       ```json
       {
         "success": true,
         "action": "created" | "updated",
         "lead": {...}
       }
       ```

    DO NOT modify the existing WhatsApp poll trigger logic - keep it for both new and updated leads.
  </action>
  <verify>
    curl -X POST http://localhost:3000/api/leads -d '{"name":"Test","phone":"5551234567","email":"test@example.com","company":"Test"}' -H "Content-Type: application/json" | grep action
    Then repeat same call - second should return "action":"updated"
  </verify>
  <done>API detects duplicates, updates existing leads with 're-apply' status, captures UTM params</done>
</task>

<task type="auto">
  <name>Create public lead form page</name>
  <files>app/lead-form/page.tsx</files>
  <action>
    Create new public route `/lead-form` (no authentication required):

    1. Create app/lead-form/page.tsx as Server Component (no layout wrapper needed, or use minimal layout)

    2. Form fields (same as LeadForm.tsx validation):
       - name (required, min 2 chars)
       - phone (required, min 10 chars)
       - email (optional, valid email format)
       - company (required, min 2 chars)
       - website (optional, valid URL)
       - message (optional, textarea)

    3. Form design considerations:
       - Clean, professional appearance using shadcn/ui components
       - Mobile responsive (single column on mobile, 2-column grid on desktop)
       - Turkish labels and placeholders
       - Success message after submission (show confirmation, optionally redirect after 3 seconds)
       - Error handling with clear messages
       - Loading state during submission

    4. Extract UTM parameters from URL query string:
       - On page load, check URL for utm_source, utm_medium, utm_campaign, utm_content, utm_term
       - Store in form state (hidden fields)
       - Include in POST request when submitting

    5. Form submission:
       - POST to /api/leads
       - Include form fields + UTM parameters
       - source: 'web_form' (hardcoded, not user-selectable)
       - status: 'new' (default)
       - Handle success/error responses

    6. Page metadata:
       - Title: "Moka CRM - Lead Formu" or similar
       - Noindex, nofollow meta tags (public form, don't index)

    DO NOT include:
    - Authentication requirement (public page)
    - Admin-only fields (status, score, quality, tags, createdBy)
    - Lead source selection (always web_form)

    Reference LeadForm.tsx for validation logic and UI patterns, but simplify for public use.
  </action>
  <verify>
    Visit http://localhost:3000/lead-form
    - Form renders without authentication
    - All fields present with Turkish labels
    - Submit with valid data creates lead (check via admin panel)
  </verify>
  <done>Public form accessible at /lead-form, creates leads with UTM tracking</done>
</task>

<task type="auto">
  <name>Add honeypot spam protection to form</name>
  <files>app/lead-form/page.tsx</files>
  <action>
    Add honeypot field to prevent bot spam:

    1. Add hidden form field with name like "website_confirm" or "fax_number"
    2. Style it to be invisible to humans (CSS: opacity: 0, position: absolute, etc.)
    3. If field has value on submission, reject as spam (don't create lead)
    4. Return 400 error without revealing anti-spam mechanism

    The honeypot field should:
    - Have a legitimate-sounding name to attract bots
    - Be hidden visually but present in DOM
    - Be validated server-side (don't create lead if filled)
    - Not affect normal form functionality

    Implement in both form rendering and submission handler.
  </action>
  <verify>
    Inspect page source, verify honeypot field exists
    Submit form with honeypot filled - should return 400 error
    Submit form normally - should succeed
  </verify>
  <done>Honeypot field protects against automated spam submissions</done>
</task>

</tasks>

<verification>
After completion, verify:
- /lead-form page loads without authentication
- Form validation matches admin form rules
- UTM parameters are captured from URL and stored with lead
- Duplicate submissions update existing lead with 're-apply' status
- Honeypot field prevents spam but doesn't affect real submissions
- Success message displays after form submission
</verification>

<success_criteria>
1. Public form accessible at /lead-form without authentication
2. Form accepts standard lead fields with proper validation
3. UTM parameters captured from URL and stored in database
4. Duplicate leads update existing record with 're-apply' status
5. Honeypot field prevents automated spam
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-lead-capture/04.1-03-SUMMARY.md`
</output>
