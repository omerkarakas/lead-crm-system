---
phase: 04.1-lead-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/lead.ts
  - components/leads/LeadForm.tsx
  - components/leads/LeadFilter.tsx
  - components/leads/LeadInfo.tsx
autonomous: true

must_haves:
  truths:
    - "LeadStatus enum includes 're-apply' value"
    - "LeadForm component shows 'Tekrar Başvuru' label for re-apply status"
    - "LeadFilter includes 're-apply' as a status filter option"
    - "LeadInfo displays 're-apply' status with appropriate badge styling"
  artifacts:
    - path: "types/lead.ts"
      provides: "LeadStatus enum with RE_APPLY constant"
      contains: "RE_APPLY = 're-apply'"
    - path: "components/leads/LeadForm.tsx"
      provides: "Status labels mapping with re-apply entry"
      contains: "[LeadStatus.RE_APPLY]: 'Tekrar Başvuru'"
  key_links:
    - from: "components/leads/LeadForm.tsx"
      to: "types/lead.ts"
      via: "import { LeadStatus }"
      pattern: "import.*LeadStatus.*from.*lead"
---

<objective>
Add new lead status value 're-apply' to track duplicate leads from Meta Ads or other sources.

Purpose: Enable tracking of leads who apply multiple times (e.g., via Facebook Lead Ads) without creating duplicate records. This preserves data integrity while showing re-engagement activity.
Output: Updated LeadStatus enum and all UI components that display status labels
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@types/lead.ts
@components/leads/LeadForm.tsx
@components/leads/LeadFilter.tsx
@components/leads/LeadInfo.tsx
</context>

<tasks>

<task type="auto">
  <name>Add RE_APPLY to LeadStatus enum</name>
  <files>types/lead.ts</files>
  <action>
    Add new enum value to LeadStatus:
    - Add `RE_APPLY = 're-apply'` after LOST in the enum
    - This represents leads who have submitted information multiple times

    Do NOT modify any other values in the enum.
  </action>
  <verify>grep "RE_APPLY = 're-apply'" types/lead.ts returns the line</verify>
  <done>LeadStatus enum includes RE_APPLY constant with value 're-apply'</done>
</task>

<task type="auto">
  <name>Update status labels in LeadForm component</name>
  <files>components/leads/LeadForm.tsx</files>
  <action>
    Add 're-apply' status label to STATUS_LABELS constant:
    - Add entry: `[LeadStatus.RE_APPLY]: 'Tekrar Başvuru'`
    - Place it after LOST entry to maintain logical grouping

    This ensures the status dropdown shows "Tekrar Başvuru" when creating/editing leads.
  </action>
  <verify>grep "Tekrar Başvuru" components/leads/LeadForm.tsx returns the label entry</verify>
  <done>LeadForm status dropdown displays "Tekrar Başvuru" option</done>
</task>

<task type="auto">
  <name>Update status labels in LeadFilter component</name>
  <files>components/leads/LeadFilter.tsx</files>
  <action>
    Add 're-apply' status label to filter options:
    - Find the status filter component (likely uses similar STATUS_LABELS pattern)
    - Add `[LeadStatus.RE_APPLY]: 'Tekrar Başvuru'` entry
    - Ensure filter includes re-apply status option

    If LeadFilter uses inline options instead of STATUS_LABELS, add the option directly.
  </action>
  <verify>grep "re-apply" components/leads/LeadFilter.tsx returns the filter option</verify>
  <done>LeadFilter status dropdown includes "Tekrar Başvuru" filter option</done>
</task>

<task type="auto">
  <name>Update status display in LeadInfo component</name>
  <files>components/leads/LeadInfo.tsx</files>
  <action>
    Add badge styling for 're-apply' status:
    - Find status badge rendering logic
    - Add case for LeadStatus.RE_APPLY with appropriate variant
    - Use 'secondary' or 'outline' variant to distinguish from other statuses

    The badge should display "Tekrar Başvuru" label when status is re-apply.
  </action>
  <verify>grep -i "re.apply\|tekrar" components/leads/LeadInfo.tsx returns the badge case</verify>
  <done>LeadInfo displays "Tekrar Başvuru" badge with appropriate styling</done>
</task>

</tasks>

<verification>
After completion, verify:
- LeadStatus enum has 6 values (new, qualified, booked, customer, lost, re-apply)
- All components that display status labels include "Tekrar Başvuru"
- TypeScript compiles without errors (no missing enum value errors)
</verification>

<success_criteria>
1. LeadStatus.RE_APPLY constant exists with value 're-apply'
2. LeadForm, LeadFilter, and LeadInfo all handle the new status correctly
3. UI displays "Tekrar Başvuru" label for re-apply status
4. No TypeScript compilation errors related to enum values
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-lead-capture/04.1-01-SUMMARY.md`
</output>
