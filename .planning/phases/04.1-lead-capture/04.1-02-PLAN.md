---
phase: 04.1-lead-capture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/config/qa.ts
  - app/api/settings/route.ts
  - app/(dashboard)/admin/settings/page.tsx
  - pb_migrations/XXXX_add_booking_link_setting.js
autonomous: true

must_haves:
  truths:
    - "app_settings collection has booking_link_url record for calcom service"
    - "Settings UI shows booking link field in Cal.com tab"
    - "lib/config/qa.ts no longer has hardcoded calcomMeetingUrl"
    - "getBookingLink() function reads from database instead of returning constant"
  artifacts:
    - path: "pb_migrations/XXXX_add_booking_link_setting.js"
      provides: "Database migration for booking_link_url setting"
      contains: "booking_link_url"
    - path: "lib/config/qa.ts"
      provides: "Updated booking link retrieval from database"
      exports: ["getBookingLink"]
  key_links:
    - from: "lib/config/qa.ts"
      to: "app_settings collection"
      via: "PocketBase query for booking_link_url"
      pattern: "pb\.collection\('app_settings'\)"
    - from: "app/(dashboard)/admin/settings/page.tsx"
      to: "app/api/settings/route.ts"
      via: "PATCH request to update setting"
      pattern: "updateSetting.*booking"
---

<objective>
Make booking link configurable via admin settings instead of hardcoded value.

Purpose: Allow admins to change the Cal.com booking URL without code deployment. The booking link is displayed in WhatsApp messages to qualified leads, so it needs to be easily configurable.
Output: Database-stored booking URL with admin UI, removed hardcoded value
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/config/qa.ts
@app/api/settings/route.ts
@app/(dashboard)/admin/settings/page.tsx
@pb_migrations/1772695621_created_app_settings.js
</context>

<tasks>

<task type="auto">
  <name>Create database migration for booking_link_url setting</name>
  <files>pb_migrations/XXXX_add_booking_link_setting.js</files>
  <action>
    Create PocketBase migration to add booking_link_url to app_settings:

    1. Create new migration file with timestamp prefix (use current timestamp)
    2. Find app_settings collection in migrate(app) function
    3. Add new field definition for booking_link_url
    4. In rollback, find and remove the field

    Field spec:
    - name: "booking_link_url"
    - type: "url"
    - required: false
    - system: false

    Reference existing migration structure from pb_migrations/1772695621_created_app_settings.js

    Then run migration: `npx pocketbase migrate`
  </action>
  <verify>grep "booking_link_url" pb_data/pb_schema.json returns the field definition</verify>
  <done>app_settings collection has booking_link_url field in database schema</done>
</task>

<task type="auto">
  <name>Seed initial booking_link_url setting</name>
  <files>app/api/settings/route.ts</files>
  <action>
    Add initial booking_link_url record to database via API or directly:

    1. Use PocketBase admin UI or API call to create setting:
       - service_name: "calcom"
       - setting_key: "booking_link_url"
       - setting_value: "https://cal.mokadijital.com/moka/30min" (current hardcoded value)
       - description: "Randevu booking link for qualified leads"
       - is_active: true

    Use either:
    - POST /api/settings with CreateSettingDto (admin-only endpoint exists)
    - Or direct PocketBase collection creation via script

    Verify the record exists after creation.
  </action>
  <verify>curl -s http://localhost:3000/api/settings | grep booking_link_url returns the setting</verify>
  <done>app_settings contains booking_link_url record with current Cal.com URL</done>
</task>

<task type="auto">
  <name>Update getBookingLink() to read from database</name>
  <files>lib/config/qa.ts</files>
  <action>
    Refactor getBookingLink() function to read from database instead of returning hardcoded constant:

    1. Remove hardcoded calcomMeetingUrl constant (line 13)
    2. Update getBookingLink() to:
       - Accept optional pb parameter for dependency injection
       - Query app_settings collection for booking_link_url
       - Return the setting value or fallback to empty string
       - Cache result in memory to avoid repeated queries (optional optimization)

    3. Update imports to include getServerPb if needed

    The function should have this signature:
    ```typescript
    export async function getBookingLink(pb?: PocketBase): Promise<string>
    ```

    For callers that need synchronous behavior, provide both:
    - getBookingLink() - async, reads from DB
    - Consider keeping a default constant for fallback

    DO NOT modify formatWelcomeMessage, formatPollMessage, calculateScore, isQualified functions.
  </action>
  <verify>grep -n "calcomMeetingUrl" lib/config/qa.ts returns no results (hardcoded value removed)</verify>
  <done>getBookingLink() reads from app_settings collection, hardcoded URL removed</done>
</task>

<task type="auto">
  <name>Update callers of getBookingLink() for async compatibility</name>
  <files>
    lib/whatsapp/message-formatter.ts
    lib/api/qa.ts
  </files>
  <action>
    Find all files that call getBookingLink() and update to handle async:

    1. Search for `getBookingLink(` across codebase
    2. Update each call site to await the async function
    3. Update parent functions to be async if needed

    Known callers (verify these):
    - lib/whatsapp/message-formatter.ts - sends booking link to qualified leads
    - Any other files that import and use getBookingLink

    For each file:
    - Change `getBookingLink()` to `await getBookingLink()`
    - Ensure function containing the call is marked async
    - Test that TypeScript compiles without errors
  </action>
  <verify>grep -r "getBookingLink" app/ lib/ | grep -v "await" | wc -l returns 0 (all calls use await)</verify>
  <done>All callers of getBookingLink() properly await the async function</done>
</task>

<task type="auto">
  <name>Add booking link field to Cal.com settings UI</name>
  <files>app/(dashboard)/admin/settings/page.tsx</files>
  <action>
    The settings form (SettingsForm component) already dynamically renders fields based on app_settings records. Since we added booking_link_url to the database, it should appear automatically.

    Verify this works by:
    1. Check that SettingsForm component loads all settings for the service
    2. Confirm booking_link_url appears in Cal.com tab
    3. Test that updating the value persists correctly

    If the field doesn't appear automatically, investigate SettingsForm component and ensure it properly handles all setting types (text, url, etc).

    The field should:
    - Show current booking URL value
    - Allow admin to edit and save
    - Validate URL format (handled by url field type in PocketBase)
  </action>
  <verify>Visit http://localhost:3000/admin/settings, click Cal.com tab, verify booking_link_url field is visible and editable</verify>
  <done>Admin settings UI displays and allows editing booking_link_url in Cal.com tab</done>
</task>

</tasks>

<verification>
After completion, verify:
- app_settings has booking_link_url record
- lib/config/qa.ts has no hardcoded calcomMeetingUrl
- getBookingLink() is async and reads from database
- Admin settings UI shows booking link field in Cal.com tab
- WhatsApp messages use the database-stored booking link
</verification>

<success_criteria>
1. Hardcoded calcomMeetingUrl removed from codebase
2. getBookingLink() retrieves URL from app_settings collection
3. Admin can change booking link via settings UI without code deployment
4. All async calls to getBookingLink() properly awaited
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-lead-capture/04.1-02-SUMMARY.md`
</output>
