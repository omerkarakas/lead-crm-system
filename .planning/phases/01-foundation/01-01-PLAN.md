---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/main.ts
  - pb_schema.json
  - src/lib/pocketbase.ts
  - src/stores/auth.ts
  - src/components/auth/LoginForm.tsx
  - src/components/auth/OAuthButton.tsx
  - src/App.tsx
autonomous: true
user_setup:
  - service: pocketbase
    why: "Backend and authentication system"
    env_vars:
      - name: VITE_POCKETBASE_URL
        source: "PocketBase server URL (local: http://127.0.0.1:8090 or production URL)"
  - service: google_oauth
    why: "Google OAuth authentication"
    env_vars:
      - name: VITE_GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> OAuth 2.0 Client IDs"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> OAuth 2.0 Client IDs (server-side)"
      - name: VITE_GOOGLE_REDIRECT_URI
        source: "Your application URL (e.g., http://localhost:5173)"
    dashboard_config:
      - task: "Create OAuth 2.0 credentials"
        location: "Google Cloud Console -> Credentials -> Create Credentials -> OAuth client ID"
  - service: github_oauth
    why: "GitHub OAuth authentication"
    env_vars:
      - name: VITE_GITHUB_CLIENT_ID
        source: "GitHub Developer Settings -> OAuth Apps -> New OAuth App"
      - name: GITHUB_CLIENT_SECRET
        source: "GitHub Developer Settings -> OAuth Apps -> Client Secret"
      - name: VITE_GITHUB_REDIRECT_URI
        source: "Your application URL (e.g., http://localhost:5173)"

must_haves:
  truths:
    - "User can log in with email and password"
    - "User can log in with Google OAuth"
    - "User can log in with GitHub OAuth"
    - "User session persists across browser refresh (PocketBase auth store)"
    - "User can log out from any page"
    - "Protected routes redirect unauthenticated users to login"
    - "PocketBase collections exist for users, leads, notes, and tags"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "pocketbase, vite, react or vue"
    - path: "pb_schema.json"
      provides: "PocketBase collection definitions"
      contains: "users collection with email, passwordHash, name, role, avatar fields"
    - path: "src/stores/auth.ts"
      provides: "Authentication state management"
      exports: ["useAuthStore", "login", "logout", "isAuthenticated"]
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Email/password login UI"
      min_lines: 50
    - path: "src/lib/pocketbase.ts"
      provides: "PocketBase client initialization"
      exports: ["pb"]
  key_links:
    - from: "src/main.ts"
      to: "src/lib/pocketbase.ts"
      via: "import and initialize PocketBase"
      pattern: "import.*pocketbase"
    - from: "src/stores/auth.ts"
      to: "src/lib/pocketbase.ts"
      via: "use pb.collection('users').authWithPassword()"
      pattern: "pb\\.collection.*auth"
    - from: "src/components/auth/LoginForm.tsx"
      to: "src/stores/auth.ts"
      via: "call login function on form submit"
      pattern: "login\\(email, password\\)"
    - from: "src/App.tsx"
      to: "src/stores/auth.ts"
      via: "check isAuthenticated for protected routes"
      pattern: "isAuthenticated.*route"
---

<objective>
Project foundation setup with authentication system that supports email/password and OAuth (Google/GitHub) login, with persistent sessions using PocketBase.

Purpose: Establish the technical foundation for Moka CRM - the frontend framework, PocketBase integration, and authentication layer that all other features will build upon.

Output: Working project with authentication, users logged in can access protected routes, session persists across refresh.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with frontend framework and PocketBase SDK</name>
  <files>package.json, vite.config.ts, src/main.ts, index.html</files>
  <action>
    1. Choose frontend framework: Vue 3 + TypeScript (use Vite for fast development, Vue for simplicity and good PocketBase integration)
    2. Create project: npm create vite@latest moka-crm -- --template vue-ts
    3. Install dependencies:
       - pocketbase (SDK)
       - vue-router (routing)
       - pinia (state management)
       - @vueuse/core (utility functions)
    4. Configure Vite proxy for PocketBase API in vite.config.ts
    5. Create basic folder structure: src/components/, src/views/, src/stores/, src/lib/, src/router/
    6. Set up environment variables template (.env.example with VITE_POCKETBASE_URL, VITE_GOOGLE_CLIENT_ID, etc.)

    Do NOT create any auth components yet - that's Task 3.
  </action>
  <verify>npm install completes without errors, npm run dev starts dev server successfully</verify>
  <done>Project initialized with Vue 3 + TypeScript + Vite, PocketBase SDK installed, folder structure created</done>
</task>

<task type="auto">
  <name>Task 2: Design and create PocketBase schema</name>
  <files>pb_schema.json, src/lib/pocketbase.ts, src/types/pocketbase.ts</files>
  <action>
    1. Create pb_schema.json defining collections:

       users collection:
       - email (text, required, unique)
       - passwordHash (text, required)
       - name (text, required)
       - role (select: admin, sales, marketing, default: sales)
       - avatar (file, optional, max 1MB)
       - createdAt (autocreated)
       - updatedAt (autocreated)

       leads collection:
       - name (text, required)
       - phone (text, required)
       - email (text, optional)
       - company (text, optional)
       - website (text, optional)
       - message (text, optional)
       - source (select: web_form, api, manual, whatsapp, default: manual)
       - status (select: new, qualified, booked, customer, lost, default: new)
       - score (number, default: 0)
       - quality (select: pending, qualified, default: pending)
       - tags (text[], optional)
       - createdBy (relation to users)
       - createdAt (autocreated)
       - updatedAt (autocreated)

       notes collection:
       - leadId (relation to leads, required)
       - userId (relation to users, required)
       - content (text, required)
       - createdAt (autocreated)

       tags collection (for reusable tag definitions):
       - name (text, required, unique)
       - color (text, optional, hex color)
       - createdAt (autocreated)

    2. Create src/lib/pocketbase.ts:
       - Initialize PocketBase client with VITE_POCKETBASE_URL
       - Export pb instance
       - Set up auth store to persist session (pb.authStore.loadFromCookie/document)

    3. Create TypeScript types in src/types/pocketbase.ts that match schema

    4. Add setup instructions in README: How to run PocketBase locally and import schema
  </action>
  <verify>pb_schema.json is valid JSON, src/lib/pocketbase.ts exports pb instance, TypeScript types compile without errors</verify>
  <done>PocketBase schema defined with all required collections and fields, TypeScript types created, PocketBase client initialized</done>
</task>

<task type="auto">
  <name>Task 3: Implement authentication (email/password + OAuth, persistent session, logout)</name>
  <files>src/stores/auth.ts, src/components/auth/LoginForm.tsx, src/components/auth/OAuthButton.tsx, src/router/index.ts, src/views/LoginView.vue, src/views/DashboardView.vue, src/App.vue</files>
  <action>
    1. Create Pinia store (src/stores/auth.ts):
       - State: user, isAuthenticated, loading
       - Actions:
         - login(email, password) using pb.collection('users').authWithPassword()
         - loginWithGoogle() using OAuth redirect
         - loginWithGitHub() using OAuth redirect
         - logout() using pb.authStore.clear()
         - initAuth() to restore session from storage
       - Getters: isAdmin, isSales, isMarketing based on user.role
       - Persist auth store using PocketBase built-in (pb.authStore.export/import)

    2. Create login form component (src/components/auth/LoginForm.vue):
       - Email and password inputs with validation
       - Show/hide password toggle
       - Error message display (invalid credentials, network error)
       - Loading state during login
       - "Remember me" checkbox (PocketBase handles this automatically with authStore persistence)
       - Call authStore.login() on submit

    3. Create OAuth button component (src/components/auth/OAuthButton.vue):
       - Props: provider ('google' | 'github'), label, icon
       - Call authStore.loginWithGoogle() or authStore.loginWithGitHub() on click
       - Handle OAuth redirect callback on mount (check URL params for code/state)

    4. Create LoginView (src/views/LoginView.vue):
       - Center card layout with branding
       - Email/password form
       - OAuth buttons (Google, GitHub)
       - Link to password reset (placeholder for now)
       - Terms/privacy links at bottom

    5. Create DashboardView (src/views/DashboardView.vue):
       - Placeholder: "Welcome, {user.name}!"
       - Show user role badge
       - Logout button

    6. Set up Vue Router (src/router/index.ts):
       - Routes: /login (LoginView), /dashboard (DashboardView, requires auth)
       - Navigation guard: check isAuthenticated before allowing access to protected routes
       - Redirect to /login if not authenticated
       - Redirect to /dashboard if already authenticated and trying to access /login

    7. Update App.vue:
       - RouterView component
       - Initialize auth store on mount (authStore.initAuth())

    OAuth Implementation:
    - Use PocketBase built-in OAuth support (pb.collection('users').authWithOAuth2())
    - Configure OAuth providers in PocketBase admin panel (Google Cloud Console, GitHub OAuth Apps)
    - Store VITE_GOOGLE_CLIENT_ID, VITE_GITHUB_CLIENT_ID in .env
    - Handle OAuth callback: check URL for ?code= and ?state= parameters
  </action>
  <verify>Run npm run dev, navigate to /, redirect to /login, log in with email/password, redirect to /dashboard, refresh page, still logged in. Click logout, redirect to /login. Test OAuth flows if providers configured.</verify>
  <done>User can log in with email/password and OAuth (Google/GitHub), session persists across refresh, logout works, protected routes enforce authentication</done>
</task>

</tasks>

<verification>
1. Run `npm run dev` - dev server starts without errors
2. Visit http://localhost:5173 - redirects to /login
3. Log in with email/password - redirects to /dashboard, shows user name and role
4. Refresh browser - still logged in (session persisted)
5. Click logout - redirects to /login
6. Try to access /dashboard directly while logged out - redirects to /login
7. Check browser storage - PocketBase auth data is stored
8. Verify TypeScript compilation: `npm run build` completes without errors
9. Test OAuth if providers configured (Google/GitHub login flows work)
</verification>

<success_criteria>
1. Project runs in development mode with `npm run dev`
2. PocketBase server is running (local or configured URL in .env)
3. User can log in with email/password and access protected routes
4. User session persists across browser refresh
5. User can log out
6. Unauthenticated users cannot access protected routes
7. OAuth buttons render and initiate OAuth flow (if env vars configured)
8. TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Frontend framework chosen (Vue 3 + TypeScript + Vite)
- PocketBase schema details
- Authentication implementation details
- OAuth configuration status
- Known issues or next steps
</output>
