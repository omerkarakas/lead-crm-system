---
phase: 03-email-communication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pb_schema.json
  - types/email.ts
  - lib/api/email.ts
  - lib/email/template-variables.ts
  - .env.local.example
autonomous: true
user_setup:
  - service: resend
    why: "Email sending provider"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Create API key"
        location: "Resend Dashboard -> https://resend.com/api-keys"
      - task: "Verify domain"
        location: "Resend Dashboard -> Domains"

must_haves:
  truths:
    - "System can send email via Resend API"
    - "Template variables are replaced with lead data"
    - "Email is logged to messages table after sending"
  artifacts:
    - path: "lib/api/email.ts"
      provides: "Email sending functions"
      exports: ["sendEmail", "sendEmailToLead"]
    - path: "lib/email/template-variables.ts"
      provides: "Variable substitution logic"
      exports: ["replaceVariables", "getVariableValue"]
    - path: "types/email.ts"
      provides: "Email TypeScript types"
      contains: "EmailMessage, SendEmailDto"
    - path: "pb_migrations/[timestamp]_created_email_messages.js"
      provides: "PocketBase collection for email logging"
      contains: "collection: email_messages"
  key_links:
    - from: "lib/api/email.ts"
      to: "https://api.resend.com/emails"
      via: "fetch with Bearer token"
      pattern: "resend\.com/emails"
    - from: "lib/email/template-variables.ts"
      to: "types/lead.ts"
      via: "Lead type import"
      pattern: "import.*Lead.*from.*types/lead"
    - from: "lib/api/email.ts"
      to: "pb_schema.json"
      via: "email_messages collection"
      pattern: "email_messages"
---

<objective>
Set up Resend email integration with template variable substitution and email logging.

Purpose: Enable manual email sending to leads with variable replacement and activity tracking.
Output: Working email API with variable substitution and logging to PocketBase.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-email-communication/03-CONTEXT.md

# Prior Phase Context
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/02-whatsapp-qualification/02-01-SUMMARY.md

# Codebase Patterns
@lib/pocketbase.ts
@lib/api/leads.ts
@lib/whatsapp/message-formatter.ts
@types/lead.ts
@pb_schema.json
</context>

<tasks>

<task type="auto">
  <name>Create email data model and types</name>
  <files>
    types/email.ts
    pb_migrations/[timestamp]_created_email_messages.js
    pb_schema.json
  </files>
  <action>
    Create types/email.ts with:
    - EmailMessage interface (id, lead_id, to_email, subject, body, template_id, status, sent_at, created)
    - EmailStatus enum (sent, delivered, failed, pending)
    - SendEmailDto interface (to, subject, html, text?)
    - SendEmailToLeadDto interface (template_id?, subject?, body?)

    Create PocketBase migration for email_messages collection:
    - lead_id: relation to leads (cascadeDelete: true)
    - to_email: text (required)
    - subject: text (required)
    - body: text (required)
    - template_id: text (optional) - reference to template
    - direction: select (incoming/outgoing) - default "outgoing"
    - status: select (sent, delivered, failed, pending) - default "pending"
    - sent_at: date (optional)
    - resend_message_id: text (optional) - for tracking

    Update .env.local.example with RESEND_API_KEY
  </action>
  <verify>
    Run migration: PocketBase automatically runs migrations on serve, check collection exists in admin panel
  </verify>
  <done>
    email_messages collection exists in PocketBase with all required fields
  </done>
</task>

<task type="auto">
  <name>Implement template variable substitution</name>
  <files>
    lib/email/template-variables.ts
  </files>
  <action>
    Create lib/email/template-variables.ts with:

    Variable mapping function that takes a Lead object and variable name:
    - {{name}} → lead.name
    - {{first_name}} → lead.name.split(' ')[0]
    - {{company}} → lead.company || ''
    - {{email}} → lead.email || ''
    - {{phone}} → lead.phone || ''
    - {{website}} → lead.website || ''
    - {{message}} → lead.message || ''
    - {{source}} → lead.source (map to Turkish: web_form→"Web Formu", api→"API", manual→"Manuel", whatsapp→"WhatsApp")
    - {{status}} → lead.status (map to Turkish: new→"Yeni", qualified→"Qualify", etc.)

    replaceVariables(template: string, lead: Lead): string function that:
    - Finds all {{variable}} patterns using regex
    - Replaces each with lead data
    - Returns substituted string

    Add unit tests pattern using simple assertions if possible, or verify manually with example data
  </action>
  <verify>
    Test with example: "Merhaba {{name}}, şirketiniz {{company}} için..." + lead data → "Merhaba Ahmet Yılmaz, şirketiniz Acme Corp için..."
  </verify>
  <done>
    Template variables are correctly replaced with lead data
  </done>
</task>

<task type="auto">
  <name>Implement Resend email sending API</name>
  <files>
    lib/api/email.ts
  </files>
  <action>
    Create lib/api/email.ts with:

    1. sendEmail(data: SendEmailDto): Resend API call
       - POST to https://api.resend.com/emails
       - Headers: Authorization: Bearer process.env.RESEND_API_KEY
       - Body: { from, to, subject, html }
       - From email: use env variable RESEND_FROM_EMAIL or default "noreply@yourdomain.com"
       - From name: use env variable RESEND_FROM_NAME or default "Moka CRM"
       - Returns Resend response with messageId

    2. logEmailMessage(data): Save to PocketBase email_messages
       - lead_id, to_email, subject, body, template_id, status, sent_at, resend_message_id

    3. sendEmailToLead(leadId: string, options: SendEmailToLeadDto): Main function
       - Fetch lead from PocketBase
       - If template_id provided: fetch template (from email_templates collection - create placeholder check)
       - Replace variables using replaceVariables()
       - Call sendEmail()
       - Log to email_messages with status="sent"
       - Update email_messages with Resend messageId
       - Return result

    Error handling: Log failed emails with status="failed" in email_messages

    Reference pattern from lib/api/whatsapp.ts for similar external API integration
  </action>
  <verify>
    - Mock Resend API call (comment out actual API call for testing)
    - Verify email is logged to email_messages with correct status
    - Check variable substitution works with real lead data
  </verify>
  <done>
    Email API can send via Resend and log to PocketBase
  </done>
</task>

</tasks>

<verification>
1. Check email_messages collection exists in PocketBase admin panel
2. Test variable substitution with sample lead data
3. Mock test email sending (verify logging without actual send)
4. Verify TypeScript types are correct (no compilation errors)
</verification>

<success_criteria>
- Email messages collection created in PocketBase
- Template variables are correctly substituted
- sendEmailToLead() function works and logs emails
- RESEND_API_KEY environment variable documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-communication/03-01-SUMMARY.md`
</output>
