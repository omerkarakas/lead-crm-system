---
phase: 03-email-communication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pb_schema.json
  - types/email.ts
  - lib/stores/email-templates.ts
  - lib/api/email-templates.ts
  - components/admin/email/TemplateForm.tsx
  - components/admin/email/TemplateList.tsx
  - app/(dashboard)/admin/email-templates/page.tsx
  - lib/utils/permissions.ts
  - components/layout/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can create email templates with rich text WYSIWYG editor"
    - "Admin can edit existing email templates with rich text editor"
    - "Admin can archive templates (not hard delete) and restore them"
    - "Templates use {{variable}} syntax with dropdown selector insertion"
    - "Template categories are extendable by admin (not fixed enum)"
    - "Admin can send test email to own address for validation"
    - "Template list supports table/card view toggle"
    - "Only admin role can access email templates page"
  artifacts:
    - path: "pb_migrations/[timestamp]_created_email_templates.js"
      provides: "PocketBase collection for email templates"
      contains: "collection: email_templates with is_deleted flag"
    - path: "lib/api/email-templates.ts"
      provides: "Template CRUD API functions"
      exports: ["fetchTemplates", "createTemplate", "updateTemplate", "archiveTemplate", "restoreTemplate"]
    - path: "lib/stores/email-templates.ts"
      provides: "Zustand store for template state"
      exports: ["useEmailTemplatesStore"]
    - path: "app/(dashboard)/admin/email-templates/page.tsx"
      provides: "Admin-only template management page"
      contains: "permission check for admin"
    - path: "components/admin/email/TemplateForm.tsx"
      provides: "Rich text template editor with WYSIWYG"
      contains: "TipTap or similar rich text editor"
    - path: "components/admin/email/VariableSelector.tsx"
      provides: "Variable dropdown selector component"
      contains: "variable insertion at cursor position"
    - path: "lib/api/email.ts"
      provides: "Test email sending function"
      exports: ["sendTestEmail"]
  key_links:
    - from: "app/(dashboard)/admin/email-templates/page.tsx"
      to: "lib/utils/permissions.ts"
      via: "canManageEmailTemplates permission check"
      pattern: "canManageEmailTemplates"
    - from: "lib/api/email-templates.ts"
      to: "pb_schema.json"
      via: "email_templates collection"
      pattern: "email_templates"
    - from: "components/admin/email/TemplateForm.tsx"
      to: "lib/email/template-variables.ts"
      via: "variable replacement example display"
      pattern: "replaceVariables"
---

<objective>
Build email template management system with admin-only CRUD operations.

Purpose: Allow admins to create and manage reusable email templates with variable support.
Output: Complete admin page for email template management.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-email-communication/03-CONTEXT.md

# Prior Phase Context
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/02-whatsapp-qualification/02-01-SUMMARY.md

# Codebase Patterns
@lib/api/qa.ts
@lib/stores/qa.ts
@app/(dashboard)/admin/qa/page.tsx
@lib/utils/permissions.ts
@components/admin/qa/QuestionBuilder.tsx
@components/admin/qa/QuestionList.tsx
@pb_schema.json
</context>

<tasks>

<task type="auto">
  <name>Create email templates data model and API</name>
  <files>
    types/email.ts (update)
    pb_migrations/[timestamp]_created_email_templates.js
    pb_schema.json
    lib/api/email-templates.ts
  </files>
  <action>
    Update types/email.ts with:
    - EmailTemplate interface (id, name, subject, body, category, is_active, is_deleted, created, updated)
    - CreateTemplateDto and UpdateTemplateDto interfaces
    - Note: category is text (not enum) to allow admin to add custom categories

    Create PocketBase migration for email_templates collection:
    - name: text (required, min 1, max 200)
    - subject: text (required, min 1, max 500)
    - body: text (required, min 1, max 10000) - stores HTML from rich text editor
    - category: text (required, default "generic") - text field for extendable categories
    - is_active: bool (required) - default true
    - is_deleted: bool (required) - default false (for archive/restore, NOT hard delete)
    - created: date (auto)
    - updated: date (auto)

    Create lib/api/email-templates.ts following pattern from lib/api/qa.ts:
    - fetchTemplates(): Get all templates where is_deleted=false, sort by category, then name
    - fetchArchivedTemplates(): Get only is_deleted=true templates
    - fetchActiveTemplates(): Get only is_active=true AND is_deleted=false templates
    - fetchTemplateById(id): Get single template
    - fetchCategories(): Get unique list of all category values (for filter dropdown)
    - createTemplate(data): Create new template
    - updateTemplate(id, data): Update template
    - archiveTemplate(id): Set is_deleted=true (NOT delete)
    - restoreTemplate(id): Set is_deleted=false
    - toggleTemplateActive(id, isActive): Update is_active field
    - sendTestEmail(templateId, testEmailAddress): Send test email using template

    All functions use pb.collection('email_templates') with proper TypeScript types
  </action>
  <verify>
    Run migration and verify email_templates collection in PocketBase admin panel
  </verify>
  <done>
    Email templates collection exists with all API functions working
  </done>
</task>

<task type="auto">
  <name>Build template management Zustand store and components</name>
  <files>
    lib/stores/email-templates.ts
    components/admin/email/TemplateForm.tsx
    components/admin/email/TemplateList.tsx
    components/admin/email/VariableSelector.tsx
    components/admin/email/ViewToggle.tsx
    components/admin/email/CardView.tsx
  </files>
  <action>
    Create lib/stores/email-templates.ts following pattern from lib/stores/qa.ts:
    - State: templates, archivedTemplates, loading, error, viewMode ('table' | 'card')
    - Actions: fetchTemplates, createTemplate, updateTemplate, archiveTemplate, restoreTemplate, toggleTemplateActive, setViewMode, clearError
    - Use pb.collection() API calls
    - Handle errors with try/catch

    Create components/admin/email/VariableSelector.tsx:
    - Props: onInsert: (variable: string) => void, editorRef: RefObject
    - Dropdown button with "Değişken Ekle" label + Variable icon
    - Dropdown menu items for each variable:
      - {{name}} - Lead'in tam adı
      - {{first_name}} - Lead'in ismi
      - {{company}} - Lead'in şirketi
      - {{email}} - Lead'in e-posta adresi
      - {{phone}} - Lead'in telefonu
      - {{website}} - Lead'in web sitesi
      - {{message}} - Lead'in mesajı
      - {{source}} - Lead'in kaynağı
      - {{status}} - Lead'in durumu
    - On click: insert {{variable}} at current cursor position in rich text editor
    - Use shadcn/ui DropdownMenu + Button

    Create components/admin/email/TemplateForm.tsx:
    - Form with fields: name (Input), subject (Input), body (Rich Text Editor), category (Combobox - see below), is_active (Switch)
    - Rich Text Editor: Use TipTap (or @tiptap/react) with:
      - Toolbar: bold, italic, underline, lists, links, headings
      - Variable insertion button that opens VariableSelector dropdown
      - Editable content area
      - 10 rows minimum height
    - Category field: Combobox (not Select) with:
      - Base categories: Welcome, Follow-up, Qualification, Generic
      - Allow typing custom category name
      - Suggest existing categories from fetchCategories()
    - Test Email button: "Test E-posta Gönder" with input for test email address
    - Edit mode: pre-fill with existing template data
    - Create mode: empty form
    - onSave callback with form data (HTML body)
    - onCancel callback
    - Use shadcn/ui form components (form, input, switch, button, dialog)
    - Turkish labels and placeholders

    Create components/admin/email/ViewToggle.tsx:
    - Toggle button group: Table icon / Card icon
    - State: viewMode ('table' | 'card')
    - Visual: Active view is highlighted
    - Use shadcn/ui ToggleGroup

    Create components/admin/email/CardView.tsx:
    - Grid layout (responsive: 1 col mobile, 2 cols tablet, 3 cols desktop)
    - Each card shows:
      - Template name (bold)
      - Category badge
      - Subject preview (truncated)
      - Body preview (first 150 chars, truncated)
      - Active status badge (green/inactive)
      - Action buttons: Edit, Archive/Restore
    - Empty state: "Henüz e-posta şablonu yok"
    - Use shadcn/ui Card component

    Create components/admin/email/TemplateList.tsx:
    - State: viewMode ('table' | 'card')
    - Show ViewToggle component
    - Table view (default): Table with columns: Name, Category, Subject preview, Active status, Actions
    - Card view: CardView component
    - Actions: Edit button, Archive button (with confirmation, NOT delete), Restore button (for archived), Toggle active switch
    - Filter section: Category filter dropdown + Name search input
    - Empty state: "Henüz e-posta şablonu yok"
    - onEdit callback
    - onArchive callback with confirmation dialog (shows "Arsivlensin mi?" message)
    - onRestore callback (for archived templates)
    - onToggleActive callback
    - Use shadcn/ui Table, Card, Input, Select components

    Follow patterns from QuestionBuilder and QuestionList
  </action>
  <verify>
    - Components render without errors
    - TypeScript compilation passes
    - Form validation works (required fields)
    - Rich text editor toolbar functions work
    - Variable selector inserts at cursor position
    - View toggle switches between table/card
  </verify>
  <done>
    Template form and list components with rich text editor and view toggle are ready
  </done>
</task>

<task type="auto">
  <name>Create admin email templates page with permissions</name>
  <files>
    app/(dashboard)/admin/email-templates/page.tsx
    lib/utils/permissions.ts (update)
    components/layout/Sidebar.tsx (update)
  </files>
  <action>
    Create app/(dashboard)/admin/email-templates/page.tsx following pattern from admin/qa/page.tsx:
    - Client component with 'use client'
    - Check user authentication and role
    - Permission check: only admin role can access
    - Use useEmailTemplatesStore for state management
    - Fetch templates on mount
    - Show TemplateList component
    - "Yeni Şablon" button opens TemplateForm dialog
    - Dialog/Modal for create/edit (use shadcn/ui Dialog)
    - CRUD handlers with toast notifications (sonner)
    - Loading state with Loader2 spinner
    - Redirect to /leads if not admin

    Update lib/utils/permissions.ts:
    - Add CAN_MANAGE_EMAIL_TEMPLATES permission constant
    - Add to ROLE_PERMISSIONS for admin role only
    - Add canManageEmailTemplates() helper function

    Update components/layout/Sidebar.tsx:
    - Add "E-posta Şablonları" menu item under Admin section
    - Icon: Mail or MailTemplate icon from lucide-react
    - Link: /admin/email-templates
    - Only show for admin role (check user.role)

    Update lib/api/email.ts with test email function:
    - sendTestEmail(templateId: string, toEmail: string): Send test email
      - Fetch template by ID
      - Use sample lead data for variable substitution (Örnek Lead, Test Şirketi, etc.)
      - Call sendEmail() with test email address
      - Return success/error result

    Use Turkish language throughout
  </action>
  <verify>
    - Page loads at /admin/email-templates
    - Non-admin users are redirected to /leads
    - Admin users can see the page and sidebar menu item
  </verify>
  <done>
    Admin email templates page is accessible and functional
  </done>
</task>

</tasks>

<verification>
1. Visit /admin/email-templates - should load for admin, redirect for non-admin
2. Create a new template with rich text editor - verify formatting buttons work
3. Insert variable using dropdown selector - verify it inserts at cursor position
4. Add custom category name - verify it's saved and appears in filter
5. Send test email to own address - verify email arrives with variables replaced
6. Toggle between table/card view - verify both layouts work
7. Archive template (NOT delete) - verify it moves to archived, not permanently deleted
8. Restore archived template - verify it reappears in active list
9. Toggle active status - verify switch works
10. Check sidebar shows "E-posta Şablonları" for admin only
</verification>

<success_criteria>
- Email templates CRUD fully functional with archive/restore (NOT hard delete)
- Rich text WYSIWYG editor with formatting toolbar
- Variable dropdown selector inserts at cursor position
- Template categories are extendable (custom categories allowed)
- Test email feature sends to admin's own address
- Table/Card view toggle works
- Only admin role can access template management
- Sidebar menu item visible to admins only
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-communication/03-02-SUMMARY.md`
</output>
