---
phase: 03-email-communication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pb_schema.json
  - types/email.ts
  - lib/stores/email-templates.ts
  - lib/api/email-templates.ts
  - components/admin/email/TemplateForm.tsx
  - components/admin/email/TemplateList.tsx
  - app/(dashboard)/admin/email-templates/page.tsx
  - lib/utils/permissions.ts
  - components/layout/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can create email templates with subject and body"
    - "Admin can edit existing email templates"
    - "Admin can delete email templates"
    - "Templates use {{variable}} syntax for placeholders"
    - "Only admin role can access email templates page"
  artifacts:
    - path: "pb_migrations/[timestamp]_created_email_templates.js"
      provides: "PocketBase collection for email templates"
      contains: "collection: email_templates"
    - path: "lib/api/email-templates.ts"
      provides: "Template CRUD API functions"
      exports: ["fetchTemplates", "createTemplate", "updateTemplate", "deleteTemplate"]
    - path: "lib/stores/email-templates.ts"
      provides: "Zustand store for template state"
      exports: ["useEmailTemplatesStore"]
    - path: "app/(dashboard)/admin/email-templates/page.tsx"
      provides: "Admin-only template management page"
      contains: "permission check for admin"
  key_links:
    - from: "app/(dashboard)/admin/email-templates/page.tsx"
      to: "lib/utils/permissions.ts"
      via: "canManageEmailTemplates permission check"
      pattern: "canManageEmailTemplates"
    - from: "lib/api/email-templates.ts"
      to: "pb_schema.json"
      via: "email_templates collection"
      pattern: "email_templates"
    - from: "components/admin/email/TemplateForm.tsx"
      to: "lib/email/template-variables.ts"
      via: "variable replacement example display"
      pattern: "replaceVariables"
---

<objective>
Build email template management system with admin-only CRUD operations.

Purpose: Allow admins to create and manage reusable email templates with variable support.
Output: Complete admin page for email template management.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-email-communication/03-CONTEXT.md

# Prior Phase Context
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/02-whatsapp-qualification/02-01-SUMMARY.md

# Codebase Patterns
@lib/api/qa.ts
@lib/stores/qa.ts
@app/(dashboard)/admin/qa/page.tsx
@lib/utils/permissions.ts
@components/admin/qa/QuestionBuilder.tsx
@components/admin/qa/QuestionList.tsx
@pb_schema.json
</context>

<tasks>

<task type="auto">
  <name>Create email templates data model and API</name>
  <files>
    types/email.ts (update)
    pb_migrations/[timestamp]_created_email_templates.js
    pb_schema.json
    lib/api/email-templates.ts
  </files>
  <action>
    Update types/email.ts with:
    - EmailTemplate interface (id, name, subject, body, category, is_active, created, updated)
    - EmailTemplateCategory enum (welcome, follow_up, qualification, generic)
    - CreateTemplateDto and UpdateTemplateDto interfaces

    Create PocketBase migration for email_templates collection:
    - name: text (required, min 1, max 200)
    - subject: text (required, min 1, max 500)
    - body: text (required, min 1, max 10000)
    - category: select (welcome, follow_up, qualification, generic) - default "generic"
    - is_active: bool (required) - default true
    - created: date (auto)
    - updated: date (auto)

    Create lib/api/email-templates.ts following pattern from lib/api/qa.ts:
    - fetchTemplates(): Get all templates with sort by category, then name
    - fetchActiveTemplates(): Get only is_active=true templates
    - fetchTemplateById(id): Get single template
    - createTemplate(data): Create new template
    - updateTemplate(id, data): Update template
    - deleteTemplate(id): Delete template
    - toggleTemplateActive(id, isActive): Update is_active field

    All functions use pb.collection('email_templates') with proper TypeScript types
  </action>
  <verify>
    Run migration and verify email_templates collection in PocketBase admin panel
  </verify>
  <done>
    Email templates collection exists with all API functions working
  </done>
</task>

<task type="auto">
  <name>Build template management Zustand store and components</name>
  <files>
    lib/stores/email-templates.ts
    components/admin/email/TemplateForm.tsx
    components/admin/email/TemplateList.tsx
  </files>
  <action>
    Create lib/stores/email-templates.ts following pattern from lib/stores/qa.ts:
    - State: templates, loading, error
    - Actions: fetchTemplates, createTemplate, updateTemplate, deleteTemplate, toggleTemplateActive, clearError
    - Use pb.collection() API calls
    - Handle errors with try/catch

    Create components/admin/email/TemplateForm.tsx:
    - Form with fields: name (Input), subject (Input), body (Textarea with 10 rows), category (Select), is_active (Switch)
    - Edit mode: pre-fill with existing template data
    - Create mode: empty form
    - Show available variables hint: "Kullanılabilir değişkenler: {{name}}, {{company}}, {{email}}, {{phone}}, {{website}}, {{message}}, {{source}}, {{status}}"
    - onSave callback with form data
    - onCancel callback
    - Use shadcn/ui form components (form, input, textarea, select, switch, button)
    - Turkish labels and placeholders

    Create components/admin/email/TemplateList.tsx:
    - Table with columns: Name, Category, Subject preview, Active status, Actions
    - Actions: Edit button, Delete button (with confirmation), Toggle active switch
    - Empty state: "Henüz e-posta şablonu yok"
    - Group by category or show category badge
    - onEdit callback
    - onDelete callback with confirmation dialog
    - onToggleActive callback

    Both components follow patterns from QuestionBuilder and QuestionList
  </action>
  <verify>
    - Components render without errors
    - TypeScript compilation passes
    - Form validation works (required fields)
  </verify>
  <done>
    Template form and list components are ready
  </done>
</task>

<task type="auto">
  <name>Create admin email templates page with permissions</name>
  <files>
    app/(dashboard)/admin/email-templates/page.tsx
    lib/utils/permissions.ts (update)
    components/layout/Sidebar.tsx (update)
  </files>
  <action>
    Create app/(dashboard)/admin/email-templates/page.tsx following pattern from admin/qa/page.tsx:
    - Client component with 'use client'
    - Check user authentication and role
    - Permission check: only admin role can access
    - Use useEmailTemplatesStore for state management
    - Fetch templates on mount
    - Show TemplateList component
    - "Yeni Şablon" button opens TemplateForm dialog
    - Dialog/Modal for create/edit (use shadcn/ui Dialog)
    - CRUD handlers with toast notifications (sonner)
    - Loading state with Loader2 spinner
    - Redirect to /leads if not admin

    Update lib/utils/permissions.ts:
    - Add CAN_MANAGE_EMAIL_TEMPLATES permission constant
    - Add to ROLE_PERMISSIONS for admin role only
    - Add canManageEmailTemplates() helper function

    Update components/layout/Sidebar.tsx:
    - Add "E-posta Şablonları" menu item under Admin section
    - Icon: Mail or MailTemplate icon from lucide-react
    - Link: /admin/email-templates
    - Only show for admin role (check user.role)

    Use Turkish language throughout
  </action>
  <verify>
    - Page loads at /admin/email-templates
    - Non-admin users are redirected to /leads
    - Admin users can see the page and sidebar menu item
  </verify>
  <done>
    Admin email templates page is accessible and functional
  </done>
</task>

</tasks>

<verification>
1. Visit /admin/email-templates - should load for admin, redirect for non-admin
2. Create a new template - verify it appears in list
3. Edit existing template - verify changes persist
4. Toggle active status - verify switch works
5. Delete template - verify confirmation and deletion
6. Check sidebar shows "E-posta Şablonları" for admin only
</verification>

<success_criteria>
- Email templates CRUD fully functional
- Only admin role can access template management
- Templates support {{variable}} syntax
- Sidebar menu item visible to admins only
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-communication/03-02-SUMMARY.md`
</output>
