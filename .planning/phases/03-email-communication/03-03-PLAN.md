---
phase: 03-email-communication
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - components/leads/EmailHistory.tsx
  - components/leads/SendEmailDialog.tsx
  - components/leads/EmailContentModal.tsx
  - lib/stores/email.ts
  - lib/api/email.ts (update)
  - app/(dashboard)/leads/[id]/page.tsx (update - add Email tab)
  - components/leads/LeadDetailActions.tsx (update - add quick send button)
autonomous: false

must_haves:
  truths:
    - "User can click 'Email gönder' button on lead detail page"
    - "Email dialog shows template selector and editable subject/body with live HTML preview"
    - "Quick send option: one-click send with last-used template"
    - "User can send email and it appears in email history"
    - "Email history is on separate 'Email' tab (not below WhatsApp)"
    - "Email history shows minimal info: Date, Subject, Status badge"
    - "Click email history item opens modal with full email content"
    - "Variables are replaced with actual lead data in preview"
  artifacts:
    - path: "components/leads/SendEmailDialog.tsx"
      provides: "Email sending modal dialog"
      contains: "template selector, quick send button, subject input, body textarea, live HTML preview"
    - path: "components/leads/EmailHistory.tsx"
      provides: "Email history display for separate tab"
      contains: "email list with date, subject, status badge, click handler for modal"
    - path: "components/leads/EmailContentModal.tsx"
      provides: "Modal for viewing full email content"
      contains: "full email subject, body, metadata display"
    - path: "lib/api/email.ts" (update)
      provides: "API function for sending from UI"
      exports: ["sendEmailToLead", "getLastUsedTemplate"]
    - path: "lib/stores/email.ts"
      provides: "Email state store for last-used template"
      exports: ["useEmailStore"]
  key_links:
    - from: "components/leads/SendEmailDialog.tsx"
      to: "lib/api/email-templates.ts"
      via: "fetchActiveTemplates for template selector"
      pattern: "fetchActiveTemplates"
    - from: "components/leads/SendEmailDialog.tsx"
      to: "lib/api/email.ts"
      via: "sendEmailToLead function call"
      pattern: "sendEmailToLead"
    - from: "components/leads/EmailHistory.tsx"
      to: "pb_schema.json"
      via: "email_messages collection query"
      pattern: "email_messages"
---

<objective>
Build email sending UI and email history display for lead detail page.

Purpose: Enable users to send manual emails to leads and view email history.
Output: Working email dialog and email history section on lead detail page.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-email-communication/03-CONTEXT.md

# Prior Phase Context
@.planning/phases/03-email-communication/03-01-SUMMARY.md
@.planning/phases/03-email-communication/03-02-SUMMARY.md

# Codebase Patterns
@app/(dashboard)/leads/[id]/page.tsx
@components/leads/WhatsAppConversation.tsx
@components/leads/ManualPollTrigger.tsx
@components/leads/LeadDetailActions.tsx
@lib/api/whatsapp.ts
@lib/api/email.ts

# Tab Pattern Reference
Look at existing tab implementation in lead detail page if any, or implement new tab-based navigation for:
- Info tab (existing)
- WhatsApp tab (existing)
- Email tab (new - this plan)
- Notes tab (existing)
</context>

<tasks>

<task type="auto">
  <name>Build email sending dialog component with quick send</name>
  <files>
    components/leads/SendEmailDialog.tsx
    lib/stores/email.ts
    lib/api/email.ts (update - getLastUsedTemplate)
  </files>
  <action>
    Create lib/stores/email.ts:
    - State: lastUsedTemplateId: string | null
    - Actions: setLastUsedTemplate(templateId: string)
    - Persist to localStorage for cross-session retention

    Update lib/api/email.ts:
    - Add getLastUsedTemplate(leadId: string): Fetch last used template for this lead
      - Query email_messages for lead_id
      - Sort by -sent_at
      - Return template_id from most recent email
      - Return null if no emails sent

    Create components/leads/SendEmailDialog.tsx as client component:

    Props:
    - leadId: string
    - lead: Lead (for variable substitution and display)
    - open: boolean (controlled by parent)
    - onOpenChange: (open: boolean) => void
    - quickSendMode?: boolean (if true, skip dialog and send immediately with last template)

    State:
    - templates: EmailTemplate[] (fetch active templates on mount)
    - lastUsedTemplate: EmailTemplate | null (fetched from getLastUsedTemplate)
    - selectedTemplate: EmailTemplate | null
    - subject: string (from template or custom)
    - body: string (from template or custom)
    - isSending: boolean
    - previewHtml: string (live HTML preview with variables replaced)

    UI Elements (shadcn/ui Dialog):
    - Header: "E-posta Gönder" title
    - Quick Send button (if lastUsedTemplate exists):
      - Icon: Zap or Send icon
      - Label: "Hızlı Gönder: [template name]"
      - Subtitle: "Son kullanılan şablonla tek tıkla gönder"
      - Click: Immediately sends email with last template, closes dialog
    - Template selector: Select dropdown with template names, or "Özel" option
    - Subject input: Editable TextField (pre-filled from template)
    - Body textarea: Editable TextArea (pre-filled from template, 10 rows)
    - Live HTML Preview panel (side-by-side or below):
      - Shows rendered email with variables replaced using actual lead data
      - Updates in real-time as subject/body changes
      - Label: "Canlı Önizleme"
    - Send button: Triggers email send, shows loading state
    - Cancel button: Closes dialog

    Template Selection Logic:
    - When template selected: pre-fill subject and body from template
    - When "Özel" selected: clear fields for manual entry
    - Show variable hint: "Kullanılabilir değişkenler: {{name}}, {{company}}, ..."

    Send Logic:
    - Call sendEmailToLead(leadId, { subject, body, template_id })
    - Show loading state during send
    - On success: save template_id to lastUsedTemplate in store, close dialog, show success toast, trigger parent refresh
    - On error: show error toast, keep dialog open

    Live Preview Logic:
    - Use replaceVariables() from lib/email/template-variables.ts
    - Pass actual lead data for substitution
    - Update preview whenever subject, body, or selectedTemplate changes
    - Render HTML in preview panel

    Follow pattern from existing modal components (UserModal, LeadModal)
  </action>
  <verify>
    - Component renders without errors
    - Template selection updates subject/body fields
    - Live preview shows variable substitution with real lead data
    - Quick send button appears when last-used template exists
    - TypeScript compilation passes
  </verify>
  <done>
    Email dialog component with quick send and live preview ready for integration
  </done>
</task>

<task type="auto">
  <name>Build email history and email content modal components</name>
  <files>
    lib/api/email.ts (update - add getEmailHistory)
    components/leads/EmailHistory.tsx
    components/leads/EmailContentModal.tsx
  </files>
  <action>
    Update lib/api/email.ts with:
    - getEmailHistory(leadId: string): Fetch email_messages for lead
      - Filter: lead_id = leadId, direction = "outgoing"
      - Sort: -sent_at (newest first)
      - Return EmailMessage[]

    Create components/leads/EmailContentModal.tsx:
    Props:
    - email: EmailMessage | null
    - open: boolean
    - onOpenChange: (open: boolean) => void

    UI Structure (shadcn/ui Dialog):
    - Header: Email subject as title, sent date as subtitle
    - Content:
      - Metadata section: To address, Sent date, Status badge
      - Body section: Full email body rendered as HTML
      - Use sanitized HTML rendering (consider using react-html-parser or similar)
    - Footer: Close button

    Styling:
    - Max width: 2xl (wide enough for email content)
    - Body rendered in scrollable container
    - Status badge with colors matching email history

    Create components/leads/EmailHistory.tsx for separate Email tab:

    Props:
    - leadId: string

    State:
    - emails: EmailMessage[]
    - isLoading: boolean
    - error: string | null
    - selectedEmail: EmailMessage | null (for content modal)
    - isContentModalOpen: boolean

    UI Structure (Card component - minimal design):
    - Header: "E-posta Geçmişi" title with Mail icon, email count badge
    - Empty state: "Henüz e-posta gönderilmedi" with Mail icon
    - Email list: Each email shows MINIMAL info only:
      - Date (formatted: DD.MM.YYYY HH:MM)
      - Subject (bold, truncated if long)
      - Status badge: Gönderildi (green), Teslim Edildi (blue), Başarısız (red)
    - List item styling:
      - Clickable entire row (opens EmailContentModal)
      - Hover effect (subtle background change)
      - Cursor pointer
      - No body preview (click to view)

    Styling:
    - White background card with border
    - List items with subtle border between
    - Status badges use shadcn/ui Badge component
    - Date formatting: new Date(sent_at).toLocaleString('tr-TR')
    - Clickable rows for opening content modal

    No auto-refresh needed (unlike WhatsApp) - emails are one-time sends
  </action>
  <verify>
    - Components render without errors
    - Email list displays correctly with status badges
    - Turkish date formatting works
    - Clicking email row opens content modal with full body
    - TypeScript compilation passes
  </verify>
  <done>
    Email history component for separate tab and content modal ready
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
    Complete email sending and history feature:
    - SendEmailDialog component for composing emails
    - EmailHistory component for viewing sent emails
    - Integration with lead detail page
  </what-built>
  <how-to-verify>
    1. Visit lead detail page (/leads/[id])
    2. Click "Email" tab to switch from WhatsApp to Email tab
    3. Verify Email tab shows email history (empty initially)
    4. Click "Email gönder" button in action buttons
    5. Verify dialog opens with template selector
    6. Verify quick send button appears if last-used template exists
    7. Select a template and verify subject/body are pre-filled
    8. Verify live HTML preview shows with actual lead data variables replaced
    9. Click send (will fail without RESEND_API_KEY - verify error handling)
    10. After successful send, verify email appears in Email tab history
    11. Click email history item and verify content modal opens with full email
    12. Verify email history shows minimal info: Date, Subject, Status badge only
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Lead detail page has separate "Email" tab (not section below WhatsApp)
2. Email tab shows email history with minimal info (Date, Subject, Status badge)
3. "Email gönder" button opens email dialog
4. Quick send button appears when last-used template exists
5. Quick send sends email immediately without opening dialog
6. Email dialog opens with template selector
7. Template selection pre-fills subject and body
8. Live HTML preview shows variable substitution with real lead data
9. Clicking email history item opens modal with full email content
10. Status badges show correct colors (green/blue/red)
11. Turkish formatting for dates
</verification>

<success_criteria>
- Users can send manual emails from lead detail page with live HTML preview
- Quick send option for last-used template (one-click send)
- Templates are available for quick email composition
- Email history is on separate "Email" tab (not below WhatsApp section)
- Email history shows minimal info: Date, Subject, Status badge
- Click email history item opens modal with full email content
- UI is consistent with existing WhatsApp conversation design
- Last-used template is persisted for quick send functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-communication/03-03-SUMMARY.md`
</output>
