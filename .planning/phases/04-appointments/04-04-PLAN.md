---
phase: 04-appointments
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - app/(dashboard)/appointments/page.tsx
  - components/appointments/AppointmentList.tsx
  - components/appointments/AppointmentFilters.tsx
  - components/appointments/AppointmentDetailModal.tsx
  - app/(dashboard)/leads/[id]/page.tsx
  - components/appointments/LeadAppointments.tsx
autonomous: false

must_haves:
  truths:
    - "User can filter appointments by date range"
    - "User can filter appointments by status"
    - "User can search appointments by lead name/phone"
    - "Appointments list updates based on filter changes"
    - "View toggle works between table and card layouts"
    - "Appointment detail modal shows full appointment info"
    - "Lead detail page shows appointment history"
    - "Filters are preserved in URL query params"
  artifacts:
    - path: "components/appointments/AppointmentFilters.tsx"
      provides: "Filter controls for date range, status, search"
      contains: "filter controls"
    - path: "components/appointments/AppointmentDetailModal.tsx"
      provides: "Modal for viewing appointment details"
      contains: "Dialog component"
    - path: "components/appointments/LeadAppointments.tsx"
      provides: "Appointment history component for lead detail"
      contains: "appointment list"
    - path: "app/(dashboard)/appointments/page.tsx"
      provides: "Appointments page with filtering"
      contains: "filter state management"
  key_links:
    - from: "components/appointments/AppointmentFilters.tsx"
      to: "app/(dashboard)/appointments/page.tsx"
      via: "onFilterChange callback"
      pattern: "onFilterChange"
    - from: "components/appointments/AppointmentList.tsx"
      to: "app/(dashboard)/appointments/page.tsx"
      via: "filtered appointments prop"
      pattern: "appointments=\\{"
    - from: "components/appointments/LeadAppointments.tsx"
      to: "app/(dashboard)/leads/[id]/page.tsx"
      via: "import statement"
      pattern: "import.*LeadAppointments"
---

<objective>
Create advanced filtering, view toggle, and detail display for appointments. Add appointment history to lead detail page.

Purpose: Complete the appointments UI with powerful filtering for finding appointments, flexible view options for different screen sizes, and appointment history on lead detail page for comprehensive view. This completes the Phase 4 appointments feature.

Output: Full-featured appointments page with filtering, view toggle, and detail modal. Lead detail page updated with appointment history.
</object>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-appointments/04-CONTEXT.md

# Relevant context from prior phases
@.planning/phases/04-appointments/04-02-SUMMARY.md (appointment list, CRUD operations)
@.planning/phases/04-appointments/04-03-SUMMARY.md (reminder system, confirmation messages)
@.planning/phases/03-email-communication/03-02-SUMMARY.md (view toggle pattern from email templates)
@.planning/phases/01-foundation/01-03a-SUMMARY.md (filter pattern from leads page, URL query params)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create appointment filters component</name>
  <files>components/appointments/AppointmentFilters.tsx</files>
  <action>
Create components/appointments/AppointmentFilters.tsx as client component:

Filter controls:
1. Date range picker (start date, end date)
   - Use native date inputs or shadcn DatePicker
   - Default: next 30 days (from today)
   - Store in state
2. Status filter (multi-select or single-select)
   - Options: Tümü, Planlandı, Tamamlandı, İptal, Yeniden Planlandı
   - Default: Tümü (no filter)
3. Search input (debounced, 300ms)
   - Search by lead name or phone
   - Use Input component with debounced onChange
4. Reset filters button
   - Clears all filters to defaults
   - Only show when filters active

Props:
- onFilterChange: (filters: AppointmentFilters) => void
- loading?: boolean

State management:
- startDate, endDate, status, search
- Debounce search input (300ms)
- Call onFilterChange when any filter changes

Filter object interface:
```typescript
interface AppointmentFilters {
  startDate?: string; // ISO date string
  endDate?: string; // ISO date string
  status?: AppointmentStatus;
  search?: string;
}
```

Pattern reference: Similar filter pattern as LeadFilter (01-03a) with debounced search.
</action>
  <verify>
Check filter component exists with:
- Date range inputs
- Status filter select
- Search input with debounce
- Reset button
- onFilterChange callback

Command: grep -E "(startDate|endDate|status|search|onFilterChange|debounce)" components/appointments/AppointmentFilters.tsx
  </verify>
  <done>
Appointment filters component created with date range, status, and search filters. Debounced search input and reset button working.
</done>
</task>

<task type="auto">
  <name>Task 2: Add view toggle and detail modal to appointment list</name>
  <files>components/appointments/AppointmentList.tsx, components/appointments/AppointmentDetailModal.tsx</files>
<action>
Update components/appointments/AppointmentList.tsx:

Add view toggle state:
- viewMode: 'table' | 'card'
- Default: 'table' (desktop), 'card' (mobile via CSS)
- Toggle button in header (Table icon / Grid icon)
- Persist in localStorage

Add row click handler:
- Clicking appointment row opens detail modal
- Pass onDetail callback prop

Create components/appointments/AppointmentDetailModal.tsx:
- Dialog modal showing full appointment details
- Display sections:
  * Lead info (name, phone, email, company) with link to lead detail
  * Date and time (formatted in Turkish)
  * Duration
  * Location / Meeting URL
  * Status (with badge)
  * Notes (if any)
  * Created/Updated timestamps
- Action buttons:
  * Edit (opens edit modal)
  * Cancel (if scheduled)
  * Complete (if scheduled)
  * Send confirmation (manual trigger)
- Props: open, onClose, appointment, onEdit, onUpdateStatus

Pattern reference: View toggle pattern from email templates (03-02), modal pattern from LeadModal (01-03b).
</action>
  <verify>
Check components exist and work:
- AppointmentList has view toggle button
- AppointmentList switches between table and card views
- AppointmentDetailModal shows full appointment details
- Detail modal has action buttons

Command: grep -E "(viewMode|viewToggle|onDetail|AppointmentDetailModal)" components/appointments/AppointmentList.tsx components/appointments/AppointmentDetailModal.tsx
  </verify>
  <done>
View toggle added to appointment list with table/card modes. Detail modal created showing full appointment info with action buttons.
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate filters and URL params into appointments page</name>
  <files>app/(dashboard)/appointments/page.tsx</files>
<action>
Update app/(dashboard)/appointments/page.tsx:

Add filter state management:
- Use useState for filters object
- Use useEffect to sync filters with URL query params
- Use useRouter.push to update URL when filters change
- Parse URL params on mount to initialize filters

URL query params:
- start: ISO date string for startDate
- end: ISO date string for endDate
- status: AppointmentStatus string
- search: search query string

Filter API integration:
- Pass filters to fetchAppointments() API call
- Debounce search filter (300ms) to avoid excessive API calls
- Show loading state while fetching

Filter display:
- Show active filters as badges (removable)
- Count of filtered results
- "Filtreleri temizle" button when filters active

Client component wrapper:
- Extract filter logic to separate client component if needed
- Keep page as server component with client wrapper

Pattern reference: URL filter pattern from leads page (01-03a).
</action>
  <verify>
Check appointments page has:
- Filter state synced with URL
- Filters passed to API call
- Active filter badges
- Clear filters button
- Debounced search

Command: grep -E "(useSearchParams|filter|start|end|status|search)" app/(dashboard)/appointments/page.tsx
  </verify>
  <done>
Filters integrated with URL query params. Active filters displayed as badges. Clear filters button works. Search debounced to 300ms.
</done>
</task>

<task type="auto">
  <name>Task 4: Add appointment history to lead detail page</name>
  <files>components/appointments/LeadAppointments.tsx, app/(dashboard)/leads/[id]/page.tsx</files>
<action>
Create components/appointments/LeadAppointments.tsx as client component:

Display:
- List of appointments for current lead
- Sorted by scheduled_at DESC (most recent first)
- Minimal info: Date, Time, Status badge, Duration
- Click row to open detail modal (AppointmentDetailModal)

Empty state:
- "Henüz randevu yok" message
- "Randevu oluştur" button (opens appointment modal with lead pre-selected)

Props:
- leadId: string
- onCreateAppointment: () => void (callback to open appointment modal)

API integration:
- Fetch appointments by lead_id from /api/appointments?lead_id={id}
- Auto-refresh every 30 seconds (similar to WhatsApp messages)
- Show loading state

Status badges:
- Same color and icon pattern as appointments list
- Quick visual scan for appointment status

Update app/(dashboard)/leads/[id]/page.tsx:
- Add "Randevular" tab to existing tab navigation
- Tab content: LeadAppointments component
- Pass leadId prop
- Handle onCreateAppointment callback (open modal with lead pre-selected)

Pattern reference: Tab pattern from lead detail page (03-03), auto-refresh pattern from WhatsApp conversation (02-04).
</action>
  <verify>
Check lead detail page has:
- Randevular tab added to navigation
- LeadAppointments component in tab content
- Appointment list showing lead's appointments
- Create appointment button with pre-selected lead

Command: grep -E "(Randevular|LeadAppointments|onCreateAppointment)" app/(dashboard)/leads/\[id\]/page.tsx components/appointments/LeadAppointments.tsx
  </verify>
  <done>
Appointment history added to lead detail page. New tab shows lead's appointments with status badges. Create appointment button pre-selects current lead.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete appointment filtering system with view toggle, detail modal, URL params, and lead detail integration</what-built>
  <how-to-verify>
1. Visit http://localhost:3000/appointments
2. Test filters:
   - Set date range (last 7 days)
   - Verify list updates
   - Set status filter to "Planlandı"
   - Verify only scheduled appointments shown
   - Enter search term (lead name)
   - Verify results filtered
   - Check URL params update (?start=...&end=...&status=...&search=...)
3. Test view toggle:
   - Click table icon
   - Verify table view
   - Click card icon
   - Verify card view
   - Check localStorage for persistence (refresh and verify preference kept)
4. Test detail modal:
   - Click on appointment row
   - Verify modal opens with full details
   - Verify all fields displayed (lead info, date, time, location, notes)
   - Test action buttons (edit, cancel, send confirmation)
5. Test lead detail integration:
   - Navigate to a lead detail page
   - Click "Randevular" tab
   - Verify lead's appointments shown
   - Click "Randevu oluştur" button
   - Verify appointment modal opens with lead pre-selected
   - Create new appointment
   - Verify it appears in lead's appointment list

Check mobile view:
- Resize browser to mobile width
- Verify card view is default
- All filters accessible
- Detail modal works
- Lead appointment tab works
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Overall phase checks:
1. Filters work for date range, status, and search
2. URL query params update when filters change
3. Filters persist on page refresh
4. View toggle switches between table and card layouts
5. View preference persists in localStorage
6. Detail modal shows full appointment information
7. Detail modal action buttons work (edit, cancel, complete, send confirmation)
8. Lead detail page shows appointment history
9. Lead appointment tab has create button with pre-selected lead
10. Mobile responsive design works for all features

Checklist:
- [ ] Date range filter works (start and end date)
- [ ] Status filter works (scheduled, completed, cancelled, rescheduled)
- [ ] Search filter works (lead name/phone)
- [ ] Filters update URL query params
- [ ] URL params restore filters on page load
- [ ] Active filters displayed as badges
- [ ] Clear filters button resets all filters
- [ ] Search debounced to 300ms
- [ ] View toggle button switches between table and card
- [ ] View preference saved to localStorage
- [ ] Clicking appointment row opens detail modal
- [ ] Detail modal shows all appointment fields
- [ ] Detail modal action buttons work
- [ ] Lead detail page has Randevular tab
- [ ] LeadAppointments component shows lead's appointments
- [ ] Create appointment button pre-selects lead
- [ ] Mobile responsive layout works
</verification>

<success_criteria>
1. User can filter appointments by date range with start and end date pickers
2. User can filter appointments by status (scheduled, completed, cancelled, rescheduled)
3. User can search appointments by lead name or phone number
4. Filters are preserved in URL and persist across page refreshes
5. View toggle allows switching between table and card layouts
6. View preference is saved and restored from localStorage
7. Clicking appointment opens detail modal with full information
8. Detail modal provides action buttons (edit, cancel, complete, send confirmation)
9. Lead detail page displays appointment history in dedicated tab
10. Create appointment button from lead detail pre-selects the lead
</success_criteria>

<output>
After completion, create `.planning/phases/04-appointments/04-04-SUMMARY.md`
</output>
