---
phase: 04.2-proposal-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pb_migrations/{timestamp}_created_proposal_templates.js
  - pb_migrations/{timestamp}_updated_leads_proposal_fields.js
  - types/proposal.ts
  - lib/api/proposal-templates.ts
  - lib/stores/proposal-templates.ts
  - components/admin/proposals/ProposalForm.tsx
  - components/admin/proposals/ProposalTemplatesList.tsx
  - components/admin/proposals/VariableSelector.tsx
  - app/(dashboard)/admin/proposal-templates/page.tsx
  - components/layout/Sidebar.tsx
  - lib/utils/permissions.ts
autonomous: true

must_haves:
  truths:
    - "Admin can create proposal template with name, description, content (TipTap/Markdown), and variable system"
    - "Admin can define custom variables beyond defaults ({name}, {company}, {budget}, etc.)"
    - "Admin can edit and delete proposal templates (soft delete for recovery)"
    - "Admin can preview proposal template with sample data filled in"
    - "Admin-only access to proposal template management page"
  artifacts:
    - path: "pb_migrations"
      provides: "proposal_templates collection and leads collection proposal fields"
      contains: "created_proposal_templates.js, updated_leads_proposal_fields.js"
    - path: "types/proposal.ts"
      provides: "ProposalTemplate TypeScript interfaces"
      exports: ["ProposalTemplate", "CreateProposalTemplateDto", "UpdateProposalTemplateDto", "ProposalResponse"]
    - path: "lib/api/proposal-templates.ts"
      provides: "Proposal template CRUD API operations"
      exports: ["getProposalTemplates", "createProposalTemplate", "updateProposalTemplate", "deleteProposalTemplate", "getProposalTemplateById"]
    - path: "app/(dashboard)/admin/proposal-templates/page.tsx"
      provides: "Admin-only proposal template management page"
      contains: "permission check, template list, create/edit modal"
  key_links:
    - from: "app/(dashboard)/admin/proposal-templates/page.tsx"
      to: "lib/api/proposal-templates.ts"
      via: "API calls for template CRUD"
      pattern: "getProposalTemplates|createProposalTemplate|updateProposalTemplate"
    - from: "components/admin/proposals/ProposalForm.tsx"
      to: "types/proposal.ts"
      via: "TypeScript type imports"
      pattern: "ProposalTemplate|CreateProposalTemplateDto"
    - from: "components/admin/proposals/VariableSelector.tsx"
      to: "lib/api/proposal-templates.ts"
      via: "Variable definitions from template system"
      pattern: "availableVariables"
---

<objective>
Create proposal template management system with TipTap/Markdown support, custom variable system, and admin-only access control.

**Purpose:** Enable admins to create reusable proposal templates with dynamic variable substitution for personalized proposals sent to leads.

**Output:**
- proposal_templates PocketBase collection with name, description, content, editor_type, variables, is_active, is_deleted fields
- leads collection updated with offer_document_url, offer_date, offer_response, offer_responded_at fields
- ProposalTemplate TypeScript types and API functions
- Admin-only proposal template management page with list view, create/edit modal, and preview functionality
- Variable selector component for inserting template variables
- TipTap/Markdown toggle for editor preference
- Soft delete pattern for template recovery
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-proposal-management/04.2-CONTEXT.md
@.planning/phases/03-email-communication/03-02-SUMMARY.md
@types/proposal.ts (to be created)
@types/email.ts (reference for template patterns)
@types/lead.ts (reference for Lead interface)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create proposal_templates collection and update leads collection</name>
  <files>pb_migrations</files>
  <action>
    1. Create PocketBase migration file for proposal_templates collection:
       - Fields: id, name (text, required), description (text), content (text, required), editor_type (select: tiptap/markdown, default: tiptap), variables (json, array of variable definitions), is_active (bool, default: true), is_deleted (bool, default: false), created, updated
       - Add indexes on: is_deleted, is_active, name
       - Variables JSON structure: [{name: "budget", label: "Bütçe", description: "Proje bütçesi", default_value: ""}]

    2. Create PocketBase migration to add proposal fields to leads collection:
       - Add fields: offer_document_url (text), offer_date (date), offer_response (select: CEVAP_BEKLENIYOR/KABUL/RED), offer_responded_at (date)
       - These fields track proposal status per lead

    3. Run migration: npx pocketbase migrate
  </action>
  <verify>
    Check PocketBase admin UI at http://localhost:8090/_/ to verify:
    - proposal_templates collection exists with all fields
    - leads collection has offer_document_url, offer_date, offer_response, offer_responded_at fields
  </verify>
  <done>
    proposal_templates collection created with name, description, content, editor_type, variables, is_active, is_deleted fields. leads collection updated with proposal tracking fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ProposalTemplate types and API functions</name>
  <files>types/proposal.ts, lib/api/proposal-templates.ts</files>
  <action>
    1. Create types/proposal.ts with:
       - enum ProposalResponse { CEVAP_BEKLENIYOR = 'cevap_bekleniyor', KABUL = 'kabul', RED = 'red' }
       - enum EditorType { Tiptap = 'tiptap', Markdown = 'markdown' }
       - interface TemplateVariable { name: string; label: string; description: string; default_value?: string }
       - interface ProposalTemplate { id, name, description, content, editor_type, variables: TemplateVariable[], is_active, is_deleted, created, updated }
       - interface CreateProposalTemplateDto { name, description, content, editor_type?, variables?, is_active? }
       - interface UpdateProposalTemplateDto { name?, description?, content?, editor_type?, variables?, is_active?, is_deleted? }

    2. Create lib/api/proposal-templates.ts with CRUD functions:
       - getProposalTemplates(pb, filters): List templates with pagination, filter by is_deleted=false by default
       - getProposalTemplateById(pb, id): Get single template
       - createProposalTemplate(pb, data): Create new template
       - updateProposalTemplate(pb, id, data): Update template
       - deleteProposalTemplate(pb, id): Soft delete (set is_deleted=true)
       - restoreProposalTemplate(pb, id): Restore deleted template (set is_deleted=false)
       - toggleActive(pb, id, is_active): Toggle template active status

    3. Add availableVariables export with default variables:
       [{name: "name", label: "İsim", description: "Lead adı"}, {name: "company", label: "Şirket", description: "Lead şirketi"}, {name: "phone", label: "Telefon", description: "Lead telefonu"}, {name: "email", label: "E-posta", description: "Lead e-posta"}, {name: "budget", label: "Bütçe", description: "Proje bütçesi"}, {name: "appointment_date", label: "Randevu Tarihi", description: "Randevu tarihi"}, {name: "appointment_time", label: "Randevu Saati", description: "Randevu saati"}]

    Reference lib/api/email-templates.ts for API patterns and lib/api/leads.ts for PocketBase patterns.
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
  </verify>
  <done>
    ProposalTemplate types defined with all required fields and enums. API functions created for all CRUD operations with proper PocketBase integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create proposal template Zustand store and UI components</name>
  <files>lib/stores/proposal-templates.ts, components/admin/proposals/ProposalForm.tsx, components/admin/proposals/VariableSelector.tsx, components/admin/proposals/ProposalTemplatesList.tsx</files>
  <action>
    1. Create lib/stores/proposal-templates.ts Zustand store:
       - State: templates, loading, error, selectedTemplate, searchQuery, filterActive
       - Actions: fetchTemplates, createTemplate, updateTemplate, deleteTemplate, restoreTemplate, toggleActive, setSearchQuery, setFilterActive, setSelectedTemplate
       - Reference lib/stores/email-templates.ts for patterns

    2. Create components/admin/proposals/VariableSelector.tsx:
       - Combobox/dropdown for selecting variables
       - Shows default variables + custom variables from template
       - On select: inserts variable at cursor position in editor
       - Display: variable label in Turkish, shows description in tooltip
       - Reference components/admin/email/VariableSelector.tsx

    3. Create components/admin/proposals/ProposalForm.tsx:
       - Form with fields: name (required), description, editor_type (select: TipTap/Markdown), content (required, rich text editor), variables (array, add/remove custom variables)
       - TipTap editor component (reuse from email templates: components/admin/email/RichTextEditor.tsx)
       - Variable selector button next to editor
       - Live preview section showing filled template with sample data
       - Save/Cancel buttons
       - Validation: name and content required
       - Reference components/admin/email/TemplateForm.tsx

    4. Create components/admin/proposals/ProposalTemplatesList.tsx:
       - Table view with columns: Name, Description, Editor Type, Variables count, Status (Active/Inactive), Created date, Actions (Edit, Delete/Restore, Toggle Active)
       - Search by name/description
       - Filter by active status
       - Pagination
       - Click row to edit
       - Reference components/admin/email/TemplateList.tsx
  </action>
  <verify>
    TypeScript compilation passes: npx tsc --noEmit
  </verify>
  <done>
    Zustand store created with all CRUD actions. UI components created for form, list, and variable selector with proper TypeScript types.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create admin proposal templates page with permissions</name>
  <files>app/(dashboard)/admin/proposal-templates/page.tsx, lib/utils/permissions.ts, components/layout/Sidebar.tsx</files>
  <action>
    1. Add CAN_MANAGE_PROPOSAL_TEMPLATES permission to lib/utils/permissions.ts (reference existing permissions)

    2. Add "Teklif Şablonları" menu item to components/layout/Sidebar.tsx for admin role (reference "E-posta Şablonları" pattern)

    3. Create app/(dashboard)/admin/proposal-templates/page.tsx:
       - Server component with permission check: redirect to /unauthorized if not admin
       - Page title: "Teklif Şablonları"
       - Show stats: Total templates, Active templates, Inactive templates
       - "Yeni Şablon" button to create new template
       - ProposalTemplatesList component
       - ProposalForm modal (open on "Yeni Şablon" or edit click)
       - Reference app/(dashboard)/admin/email-templates/page.tsx

    4. Ensure all components use Turkish labels and messages
  </action>
  <verify>
    - Visit http://localhost:3000/admin/proposal-templates and verify:
      - Page loads without errors
      - Admin can see template list
      - Non-admin users are redirected to unauthorized page
      - "Yeni Şablon" button opens form modal
      - Turkish labels are displayed correctly
  </verify>
  <done>
    Admin-only proposal templates page accessible at /admin/proposal-templates with full CRUD functionality, permission checks, and Turkish UI.
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **Collection Verification:**
   - proposal_templates collection exists in PocketBase with all required fields
   - leads collection has proposal tracking fields (offer_document_url, offer_date, offer_response, offer_responded_at)

2. **TypeScript Verification:**
   - npx tsc --noEmit passes with no compilation errors

3. **UI Verification:**
   - Admin can access /admin/proposal-templates page
   - Admin can create new proposal template with TipTap editor
   - Admin can insert variables into template content
   - Admin can preview template with sample data
   - Admin can edit existing template
   - Admin can delete template (soft delete)
   - Admin can restore deleted template
   - Admin can toggle template active status

4. **Permission Verification:**
   - Non-admin users cannot access proposal templates page (redirect to unauthorized)
   - Only admin role sees "Teklif Şablonları" menu item
</verification>

<success_criteria>
**Plan complete when:**
1. Admin can create proposal template with name, description, and TipTap/Markdown content
2. Admin can insert variables (default: {name}, {company}, {budget}, etc.) into template
3. Admin can define custom variables beyond defaults
4. Admin can preview template with sample data
5. Admin can edit, delete (soft delete), restore, and toggle active status for templates
6. Only admin role can access proposal templates management page
7. leads collection has proposal tracking fields for future plans
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-proposal-management/04.2-01-SUMMARY.md`
</output>
