---
phase: 04.2-proposal-management
plan: 03
type: execute
wave: 2
depends_on: ["04.2-02"]
files_modified:
  - app/api/proposals/respond/route.ts
  - lib/api/proposals.ts
  - lib/api/notifications.ts
  - lib/utils/whatsapp.ts
  - app/settings/notification-settings/page.tsx
autonomous: true

must_haves:
  truths:
    - "Lead response updates proposal record (response, response_comment, responded_at)"
    - "Lead response updates lead record (offer_response, offer_responded_at)"
    - "Sales team receives WhatsApp notification when lead accepts proposal"
    - "Sales team receives WhatsApp notification when lead rejects proposal"
    - "Response notifications include lead info and proposal link"
    - "Response history tracked with full audit trail"
  artifacts:
    - path: "app/api/proposals/respond/route.ts"
      provides: "Proposal response API with notification sending"
      exports: ["POST handler for response submission"]
    - path: "lib/api/notifications.ts"
      provides: "Sales team notification API"
      exports: ["sendProposalResponseNotification"]
    - path: "lib/utils/whatsapp.ts"
      provides: "WhatsApp message formatting utilities"
      exports: ["formatProposalAcceptNotification", "formatProposalRejectNotification"]
  key_links:
    - from: "app/api/proposals/respond/route.ts"
      to: "lib/api/notifications.ts"
      via: "sendProposalResponseNotification call"
      pattern: "sendProposalResponseNotification"
    - from: "lib/api/notifications.ts"
      to: "lib/api/whatsapp.ts"
      via: "sendWhatsAppMessage call"
      pattern: "sendWhatsAppMessage"
---

<objective>
Implement proposal response tracking with sales team notifications and full audit trail.

**Purpose:** Ensure sales team is immediately notified when leads respond to proposals, and all response history is tracked for reference.

**Output:**
- Enhanced proposal response API with notification sending
- Sales team notification system via WhatsApp
- Response history tracking with timestamps and comments
- Notification settings configuration for sales team
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-proposal-management/04.2-CONTEXT.md
@.planning/phases/04.2-proposal-management/04.2-02-SUMMARY.md
@.planning/phases/02-whatsapp-qualification/02-02-SUMMARY.md
@lib/api/whatsapp.ts
@lib/api/proposals.ts
@types/proposal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create proposal response tracking with notification system</name>
  <files>app/api/proposals/respond/route.ts, lib/api/notifications.ts, lib/utils/whatsapp.ts</files>
  <action>
    1. Update app/api/proposals/respond/route.ts (created in 04.2-02):
       - Accept: { token, response, comment? }
       - Validate: token exists, not expired, not already responded (check proposal.responded_at)
       - Update proposal record:
         * response: ProposalResponse (KABUL/RED)
         * response_comment: comment text (optional)
         * responded_at: current timestamp
       - Update lead record:
         * offer_response: same as proposal.response
         * offer_responded_at: current timestamp
       - Call notification API to alert sales team
       - Return: { success, message, proposal_id, lead_id }
       - Log response for audit trail

    2. Create lib/api/notifications.ts:
       - async function sendProposalResponseNotification(pb, proposalId: string, response: ProposalResponse, lead: Lead): Send notification to sales team
       - Get sales team phone numbers from users collection (filter by role='sales' or 'admin')
       - Format notification message based on response (accept/reject)
       - Send WhatsApp message to each sales team member
       - Include: lead name, company, response, comment (if reject), proposal link
       - Handle WhatsApp errors gracefully (log but don't throw)
       - Return: { success, notified_count, errors }

    3. Create lib/utils/whatsapp.ts notification formatters:
       - formatProposalAcceptNotification(lead: Lead, proposalLink: string): string
         * Template: "🎉 Harika haber! {lead_name} ({lead_company}) teklifi kabul etti. Teklif: {link}"
       - formatProposalRejectNotification(lead: Lead, comment?: string): string
         * Template: "❌ {lead_name} ({lead_company}) teklifi reddetti. {reason ? 'Sebep: ' + comment : 'Sebep belirtilmedi.'}"
       - Format messages in Turkish with emoji for visual clarity

    Reference lib/api/whatsapp.ts for Green API patterns and fire-and-forget error handling.
  </action>
  <verify>
    - Submit test proposal response via API
    - Check proposal record updated with response, comment, responded_at
    - Check lead record updated with offer_response, offer_responded_at
    - Check WhatsApp notification received by sales team
    - Check notification includes correct lead info and response
  </verify>
  <done>
    Proposal response API updates both proposal and lead records. Sales team notification system sends WhatsApp messages on response. Notification messages formatted in Turkish with lead info and proposal link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add response history tracking and audit trail</name>
  <files>lib/api/proposals.ts, components/proposals/ProposalHistoryView.tsx</files>
  <action>
    1. Update lib/api/proposals.ts:
       - getProposalHistory(pb, proposalId): Get full history for a proposal (including original sent and responses)
       - getLeadProposalHistory(pb, leadId): Get all proposals with responses for a lead
       - Return structure includes: created_at, sent_at, responded_at, response, response_comment, token, expires_at

    2. Note: Proposals collection already tracks all responses via response, response_comment, responded_at fields. The audit trail is built-in.

    3. Create components/proposals/ProposalHistoryView.tsx:
       - Display proposal history with timeline view
       - Show: sent date, template used, response date, response type, comment
       - Color-code responses: KABUL (green), RED (red), CEVAP_BEKLENIYOR (yellow)
       - Show response timestamps in Turkish format
       - Link to view proposal (opens proposal page)
       - Reference components/leads/WhatsAppTab.tsx for timeline patterns

    4. Update components/leads/LeadDetailProposalsTab.tsx (from 04.2-02):
       - Replace basic table with ProposalHistoryView component
       - Show timeline of all proposals with responses
       - Add filter for response status (All, Pending, Accepted, Rejected)
       - Add sort by date (newest/oldest)
  </action>
  <verify>
    - Visit lead detail page Teklif tab
    - Verify proposal history shows timeline view
    - Verify response status badges display correctly
    - Verify timestamps show in Turkish format
    - Verify filter works (Pending, Accepted, Rejected)
  </verify>
  <done>
    Proposal history API returns full audit trail. Timeline view component shows all proposals with responses, timestamps, and color-coded status badges.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add notification settings configuration</name>
  <files>app/api/settings/route.ts, lib/stores/settings.ts, components/admin/settings/SettingsForm.tsx</files>
  <action>
    1. Update app/api/settings/route.ts to support notification settings:
       - Add notification settings to app_settings collection or use existing settings system
       - Settings: proposal_notification_enabled (bool), proposal_notification_phones (text array)
       - GET /api/settings returns current notification settings
       - POST /api/settings updates notification settings
       - POST /api/settings/test/proposal-notification sends test notification

    2. Update lib/stores/settings.ts:
       - Add notification settings to state
       - Add actions for updating notification settings
       - Add action for testing notification

    3. Update components/admin/settings/SettingsForm.tsx:
       - Add "Bildirimler" tab or section
       - Add toggle for proposal notifications (enabled/disabled)
       - Add phone number list for sales team (comma-separated)
       - Add "Test Bildirimi Gönder" button
       - Reference existing settings form patterns (Green API, Cal.com, Resend tabs)

    Note: This is optional enhancement - core functionality uses users collection for sales team detection. Settings provide override capability.
  </action>
  <verify>
    - Visit admin settings page and verify Bildirimler section exists
    - Toggle proposal notifications and verify setting saves
    - Add phone numbers and verify setting saves
    - Send test notification and verify WhatsApp message received
  </verify>
  <done>
    Notification settings configuration added to admin settings. Sales team can be configured via users collection or overridden with custom phone numbers. Test notification available for verification.
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **Response Tracking Verification:**
   - Proposal response updates proposal.record (response, comment, responded_at)
   - Proposal response updates lead record (offer_response, offer_responded_at)
   - Response history shows in timeline view
   - Response cannot be changed (one-time response enforced)

2. **Notification Verification:**
   - Sales team receives WhatsApp notification on proposal accept
   - Sales team receives WhatsApp notification on proposal reject
   - Notification includes lead name, company, response, and proposal link
   - Notification includes rejection comment if provided
   - Multiple sales team members notified

3. **Settings Verification:**
   - Admin can configure proposal notification settings
   - Admin can add custom phone numbers for notifications
   - Admin can send test notification
   - Notification settings persist in database
</verification>

<success_criteria>
**Plan complete when:**
1. Lead response tracked in both proposal and lead records
2. Sales team notified via WhatsApp on proposal response
3. Response history visible with full audit trail
4. Notifications include lead info and proposal link
5. Admin can configure notification settings
6. Test notification available for verification
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-proposal-management/04.2-03-SUMMARY.md`
</output>
